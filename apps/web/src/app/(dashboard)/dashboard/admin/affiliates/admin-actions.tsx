export function AffiliateAdminActions(_props: Record<string, unknown>) {
  return (
    <span className="text-muted-foreground text-xs" role="status">
      Read-only — write controls pending atomic audit integration
    </span>
  )
}
