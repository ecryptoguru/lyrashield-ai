<section aria-labelledby="hero-heading" class="relative overflow-hidden px-4 pt-16 pb-24 sm:px-6 lg:px-8">
  <div class="mx-auto max-w-6xl">
    <div class="grid gap-12 lg:grid-cols-2 lg:items-center">
      <div class="max-w-2xl">
        <span class="mb-4 inline-flex items-center rounded-full border border-accent-dim bg-accent-dim/30 px-3 py-1 text-xs font-medium text-accent">
          Early access — pre-launch
        </span>
        <h1 id="hero-heading" class="text-4xl font-semibold tracking-tight text-text sm:text-5xl lg:text-6xl">
          Secure AI-built apps before they ship.
        </h1>
        <p class="mt-6 text-lg leading-relaxed text-text-muted">
          AI wrote your code in minutes. LyraSec AI finds the vulnerabilities it shipped with, verifies they're real, opens the fix PR, and retests until they're gone. One loop, from vibe-coded to verified.
        </p>
        <div class="mt-8 flex flex-wrap items-center gap-4">
          <a
            href="#waitlist"
            class="rounded-lg bg-accent px-5 py-3 text-sm font-medium text-bg hover:bg-accent/90 focus:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2 focus-visible:ring-offset-bg"
            data-cta-id="hero_primary"
          >
            Join the waitlist
          </a>
          <a
            href="#how-it-works"
            class="rounded-lg border border-border px-5 py-3 text-sm font-medium text-text hover:bg-bg-raised focus:outline-none focus-visible:ring-2 focus-visible:ring-accent"
            data-cta-id="hero_secondary"
          >
            See how the loop works ↓
          </a>
        </div>
        <p class="mt-4 text-sm text-text-muted">No spam. One email when your invite is ready.</p>
      </div>

      <div class="relative">
        <div class="rounded-lg border border-border bg-bg-raised p-1 shadow-lg">
          <div class="flex items-center gap-2 rounded-t-md bg-border px-3 py-2">
            <div class="h-3 w-3 rounded-full bg-danger/80" aria-hidden="true"></div>
            <div class="h-3 w-3 rounded-full bg-text-muted/80" aria-hidden="true"></div>
            <div class="h-3 w-3 rounded-full bg-accent/80" aria-hidden="true"></div>
            <span class="ml-auto text-xs text-text-muted">product illustration</span>
          </div>
          <div class="rounded-b-md bg-bg p-4 font-mono text-sm text-text-muted">
            <p class="text-accent">$ lyra</p>
            <p class="mt-2">target: https://api.example.com/v1</p>
            <p class="mt-1">probing endpoints...</p>
            <p class="mt-1">verified finding: ssrf in /webhook</p>
            <p class="mt-1">opening fix PR... done</p>
            <p class="mt-1 text-accent">retest passed. vulnerability gone.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
