<section aria-labelledby="problem-heading" class="px-4 py-24 sm:px-6 lg:px-8">
  <div class="mx-auto max-w-3xl text-center">
    <h2 id="problem-heading" class="text-3xl font-semibold tracking-tight text-text sm:text-4xl">
      AI writes code faster than anyone reviews it.
    </h2>
    <p class="mt-6 text-lg leading-relaxed text-text-muted">
      Copilots and coding agents ship features in minutes, and quietly ship the classic mistakes with them: SSRF checks that string-match instead of resolving DNS, role matrices that exist but never get enforced, secrets that land in commits. Not exotic zero-days. The ordinary bugs attackers look for first, produced at machine speed.
    </p>
    <p class="mt-4 text-lg leading-relaxed text-text-muted">
      Traditional scanners weren't built for this loop. They run after the fact, flood you with unverified findings, and speak a language only security teams read.
    </p>
  </div>
</section>
