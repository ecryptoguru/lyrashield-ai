---
title: Cline MCP security workflow for AI built apps
description: Connect Cline to LyraShield MCP for bounded scans, evidence-state findings, reviewed fix proposals, retests, and a diff-aware SARIF gate.
pubDate: "2026-07-30"
author: lyrashield-team
tags: [vibe-coding-security, agent-security, verification, supply-chain]
draft: false
heroImage: agent-security-16
faq:
  - q: "Where does Cline store MCP config?"
    a: "Cline stores MCP servers in cline_mcp_settings.json under mcpServers with command, args, env, autoApprove, disabled, and type for streamable HTTP remote mode."
  - q: "How do I use LyraShield remote with Cline?"
    a: "Use type streamableHttp with URL https://app.lyrashieldai.com/api/mcp and Authorization Bearer LYRASHIELD_API_KEY. Local uses npx -y @lyrashield/mcp via stdio."
  - q: "What is LyraShield build status?"
    a: "Bounded scans, evidence states, fix-proposal recording, retest queueing, and SARIF are available now. No current MCP tool edits code or creates a branch or pull request."
  - q: "Should I use autoApprove for LyraShield tools?"
    a: "Use autoApprove sparingly. Keep read only tools like lyrashield_list_targets and lyrashield_get_findings allowed, but gate fix and retest tools behind a prompt so the agent cannot run them without your confirmation. Every auto approved tool is an action the agent takes autonomously."
---

Fastest path: run `npx lyrashield init` to detect Cline and write the MCP config, then `npx lyrashield doctor` to verify and `npx lyrashield login` to store your credentials. The manual config below is what the CLI writes, kept for reference.

You can run secure AI app development in Cline by adding LyraShield AI as an MCP server, getting findings with their recorded evidence states and supporting evidence inside Cline, and enforcing the same checks in CI with a diff aware GitHub Action that uploads SARIF.

Cline automates file edits and terminal actions, which speeds up feature work but can introduce secrets, injection, and vulnerable dependencies when scaffolding is agent driven. The [vibe coding security guide](/blog/vibe-coding-security-guide) explains why verification must be part of the agent loop. LyraShield AI is agent native AppSec for AI built software with a loop from Target to Scan to Evidence State to Fix Proposal to Retest to Assurance Report. This guide gives exact Cline config using `npx -y @lyrashield/mcp` and [https://app.lyrashieldai.com/api/mcp](https://app.lyrashieldai.com/api/mcp), verification, inline checks, and the GitHub gate. Findings retain their recorded evidence states; independent verification requires a separate receipt. Current LyraShield tools record fix proposals and queue retests but do not edit code, create branches, or open pull requests.

## 1. Prerequisites and setup

You need Cline extension 3.12 or newer in VS Code or compatible editor, Node 18 or newer, a LyraShield key `lsk_` for remote mode, and permission to add workflows. Cline reads MCP from `cline_mcp_settings.json` under `mcpServers` with keys such as `command`, `args`, `env`, `autoApprove`, `disabled`, and `type`. The `command` and `args` keys launch a local stdio server, `env` injects variables that Cline expands at runtime, and `type` selects the transport. Use `type: "streamableHttp"` for remote servers that expose a URL endpoint. The `disabled` key lets you toggle a server without deleting its entry. The editor docs and the [Cline documentation](https://docs.cline.bot/mcp/mcp-overview) describe this file, and the canonical steps are in the [Cline integration guide](/docs/integrations/cline). Restart Cline after saving the server configuration, then confirm the tool list before your first scan. Review baseline risks via [OWASP Top 10](https://owasp.org/www-project-top-ten/) and dependency advisories via [OSV](https://osv.dev/). For protocol basics, see [Model Context Protocol intro](https://modelcontextprotocol.io/docs/getting-started/intro).

## 2. Configure Cline MCP servers

Create or edit `cline_mcp_settings.json` to add both local stdio and remote HTTP modes. Use `autoApprove` carefully, `disabled` to toggle, and `type` for remote.

Local stdio:

```json
{
  "mcpServers": {
    "lyrashield": {
      "command": "npx",
      "args": ["-y", "@lyrashield/mcp"],
      "env": {
        "LYRASHIELD_API_KEY": "${LYRASHIELD_API_KEY}"
      },
      "disabled": false,
      "autoApprove": []
    }
  }
}
```

Remote HTTP streamable:

```json
{
  "mcpServers": {
    "lyrashield-remote": {
      "type": "streamableHttp",
      "url": "https://app.lyrashieldai.com/api/mcp",
      "headers": {
        "Authorization": "Bearer ${LYRASHIELD_API_KEY}"
      },
      "disabled": false,
      "autoApprove": ["lyrashield_get_scan_status", "lyrashield_list_targets"]
    }
  }
}
```

Remote URL is [https://app.lyrashieldai.com/api/mcp](https://app.lyrashieldai.com/api/mcp) with Bearer `lsk_`. Both configs use the pinned command `npx -y @lyrashield/mcp` for stdio. Cline expands `${VAR}` from your env, so keep secrets out of the file itself. Link config as [Cline integration guide](/docs/integrations/cline) to avoid bare internal paths. For related editor checklists, the [Cline AI app security checklist](/blog/cline-app-security-checklist) provides a compatible rollout pattern and the [Replit app security checklist](/blog/replit-app-security-checklist) shows a similar diff gate adapted for web IDEs.

The `autoApprove` array controls which tool names run without your confirmation. Every entry you list there executes immediately, so treat it as a privilege grant. For a security scanner, keep `autoApprove` empty or limited to read only tools like `lyrashield_get_scan_status` and `lyrashield_list_targets`, and leave fix and retest tools gated behind your click.

## 3. Verify the connection in Cline

Reload VS Code after editing `cline_mcp_settings.json`, then open Cline and open MCP servers panel. You should see `lyrashield` and `lyrashield-remote` listed as active. Call "scan status" from the tool list. If auth fails, verify that `LYRASHIELD_API_KEY` is exported in the terminal that launched VS Code, because Cline inherits env from the host. If the server stays `disabled`, set `disabled: false` and reload. The [Cline MCP docs](https://docs.cline.bot/mcp/mcp-overview) note that `autoApprove` can hide confirmations, so keep it minimal for security tools. For general MCP troubleshooting, the [MCP spec](https://modelcontextprotocol.io/specification/2025-03-26) describes tool discovery and error shapes.

A common failure is a silent key mismatch. If you launched VS Code from a desktop shortcut instead of a terminal where `LYRASHIELD_API_KEY` is exported, Cline sees an empty string and returns auth errors. Confirm the variable with `printenv LYRASHIELD_API_KEY` before relaunching. If remote mode connects but tool calls time out, verify that your network allows outbound HTTPS to `https://app.lyrashieldai.com`.

## 4. Run an inline security check from Cline

Prompt Cline: "using LyraShield MCP, scan the authorized target, preserve every finding’s evidence state, and group findings by auth, injection, secrets, and supply chain." Use `lyrashield_scan_target` for the scan, `lyrashield_get_findings` for results, `lyrashield_record_fix_proposal` for the intended remediation, and `lyrashield_verify_fix` only after a developer applies the change.

Example rules you can store in `.clinerules/security.md`:

```md
# Security loop for Cline

- Define Target: app root, entry points, sensitive stores
- Scan via LyraShield MCP before finalizing large edits
- Treat findings with their recorded evidence states and supporting evidence as blocking
- Create fix proposal summary, include tests, request retest
- Append Report with scan ID and SARIF link
```

An approval-gated fix proposal is a recorded remediation summary, not a patch, branch, pull request, or merge. The developer applies the code change; `lyrashield_verify_fix` then queues a retest and returns a reference to poll.

## 5. Wire the GitHub Action diff aware gate

Add `.github/workflows/lyrashield.yml` with SARIF upload, as per [GitHub docs for SARIF upload](https://docs.github.com/en/code-security/code-scanning/integrating-with-code-scanning/uploading-a-sarif-file-to-github).

```yaml
name: lyrashield diff gate
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
  security-events: write
  pull-requests: write
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - uses: ecryptoguru/lyrashield-ai@v1
        with:
          fail_on_severity: HIGH
```

This workflow runs the published action's local diff advisory on the pull-request changes. It does not call the remote MCP endpoint, use Bearer authentication, or require `LYRASHIELD_API_KEY`, and its heuristic output is not a recorded scan or an independent-verification receipt.

The diff-aware design matters because Cline sessions often touch dozens of files in a single prompt. The action evaluates the changed lines supplied by the pull-request context and applies the configured severity threshold. Treat it as a fast advisory gate; use a separate recorded LyraShield scan when you need persisted findings, evidence states, coverage receipts, or an assurance report.

## 6. Add agent rules snippet for team use

Commit a shared rule file so all contributors get the same behavior. This also helps new engineers link the pillar doc correctly as [vibe coding security guide](/blog/vibe-coding-security-guide) rather than a bare URL.

```md
# Team security rules

- Always include Target description in PRs that touch auth or data.
- Run LyraShield scan via MCP before requesting review.
- Include evidence for each finding with a recorded evidence state in PR body.
- Record a fix proposal with minimal diff, add regression test.
- Link SARIF artifact and scan ID in Report footer.
```

This codifies consistent security behavior between Cline, GitHub, and other editors. For deeper supply chain context, see [NIST SSDF](https://csrc.nist.gov/Projects/ssdf) and [OWASP Top 10](https://owasp.org/www-project-top-ten/) as primary sources.

Agent rules work because Cline reads `.clinerules` files at the start of every session and treats them as standing instructions. When you commit `.clinerules/security.md` to the repository, every contributor gets the same scan requirement without reading a wiki page. Keep rules short and imperative, five to seven lines maximum, because long rule files dilute attention. Pair the rules with a PR template that asks for the scan ID and SARIF link.

| Mode    | Config file                      | Launch style                 | Auth                                        |
| ------- | -------------------------------- | ---------------------------- | ------------------------------------------- |
| Local   | cline_mcp_settings.json          | npx -y @lyrashield/mcp       | env LYRASHIELD_API_KEY                      |
| Remote  | cline_mcp_settings.json          | streamableHttp               | Bearer https://app.lyrashieldai.com/api/mcp |
| CI gate | .github/workflows/lyrashield.yml | ecryptoguru/lyrashield-ai@v1 | no LyraShield secret required               |

## 7. Why pair Cline with LyraShield

Cline gives you autonomous edit loops, LyraShield adds verification that is designed to be proof based and reportable. The [vibe coding security guide](/blog/vibe-coding-security-guide) explains how fast agent loops without verification raise systemic risk across auth, injection, and secrets. The core problem is speed without proof: Cline can scaffold a full feature in minutes, but every generated file is unverified until someone reviews it. Pairing keeps your flow in Cline but your policy in one place: findings with recorded evidence states, recorded fix proposals, retest, and SARIF reports that GitHub understands per [docs.github.com on code scanning](https://docs.github.com/en/code-security/code-scanning/introduction-to-code-scanning). LyraShield v1 includes agentic pentest plus SCA plus secrets plus GitHub Action plus SARIF. Bounded scans, evidence states, fix-proposal recording, retest queueing, and SARIF are available now.

To start, add Cline MCP servers, verify tool list, run a diff scan on your next AI generated branch, triage one finding with its recorded evidence state and supporting evidence, record a fix proposal with tests, and confirm SARIF in GitHub code scanning. Expand coverage using the [AI app security checklist](/tools/ai-app-security-checklist) and [create an account at LyraShield AI](https://lyrashieldai.com) to follow when execution becomes live.
