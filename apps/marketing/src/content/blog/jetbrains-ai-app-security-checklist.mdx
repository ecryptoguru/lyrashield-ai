---
title: JetBrains AI App Security Checklist for Vibe Coding
description: Secure JetBrains AI Assistant workflows with MCP hardening plus secrets scanning and verified review before every merge.
pubDate: 2026-07-30
author: lyrashield-team
tags: [vibe-coding-security, agent-security, verification, supply-chain]
draft: false
heroImage: agent-security-11
faq:
  - q: How does JetBrains AI Assistant handle MCP servers securely
    a: JetBrains AI Assistant reads MCP config as JSON and spawns local servers via stdio or connects to remote with TLS and Bearer auth. Lock it by allow listing only required tools, disabling code mode when unused, and storing tokens in env vars not in JSON.
  - q: Do I need LyraShield to use JetBrains AI Assistant safely
    a: You can ship safer code with disciplined review and open source scanners. LyraShield is designed to add agent native verification with SCA and secrets checks plus a diff aware gate on the roadmap.
  - q: What is the fastest win for JetBrains AI Assistant security
    a: Rotate leaked secrets, enforce branch protection with SARIF upload, and restrict MCP tool permissions to read only where possible to reduce blast radius.
---

When you ship apps with JetBrains AI Assistant, AI generated code can introduce secrets, vulnerable dependencies, and weak auth that reach production fast. This checklist gives a direct security review for JetBrains AI Assistant, from MCP hardening to merge gates and retest, with verified configs you can copy. Pair it with our pillar guide at [vibe coding security guide](/blog/vibe-coding-security-guide) and apply it on every pull request.

## How JetBrains AI Assistant connects via MCP

JetBrains AI Assistant uses Model Context Protocol to let the agent read project files, call tools, and run commands. That power expands your attack surface because a misconfigured MCP server can read secrets, write files, or exfiltrate code. Keep scope tight and treat all external tool output as untrusted data.

The official MCP spec defines how tool listing and invocation works over stdio and streamable HTTP, including transport security and tool permissions [MCP spec for tools](https://modelcontextprotocol.io/specification/2025-06-18/server/tools). For LLM integrations, OWASP highlights excessive agency and tool output handling as key risks to review before you enable write actions [JetBrains MCP docs](https://www.jetbrains.com/help/ai-assistant/mcp.html). Hard coded credential risks map to [CWE 798 for hard coded credentials](https://cwe.mitre.org/data/definitions/798.html), a frequent failure in AI suggested code. If you decouple spec from enforcement, review drift happens quickly. For a comparison of IDE side controls, see our review of bolt app security checklist at [bolt app security checklist](/blog/bolt-app-security-checklist).

## Locking down MCP configuration for JetBrains AI Assistant

JetBrains AI Assistant supports MCP as JSON Level Project and Global. In Settings go to Tools then AI Assistant then MCP Servers, then choose As JSON. Project level JSON lives under .idea or team shared config, global level lives in IDE user config directory. Project scope is preferred for team repos so changes are reviewed in pull requests. Vendor guidance is at [JetBrains MCP docs](https://www.jetbrains.com/help/ai-assistant/mcp.html).

Secure it in four practical steps.

First, never inline secrets in JSON. Use `${VAR}` references and load values from env or your secret manager, with a .env file gitignored and keys rotated monthly.

Second, set allow lists explicitly. Prefer empty auto approve and then include only read only tools you need. If the tool supports trust or codemode flags, disable code execution unless required and require explicit user approval for file writes.

Third, pin versions and audit local servers. For stdio, use `npx -y @lyrashield/mcp` only after you pin a version and check publish provenance. For remote, require TLS and Bearer auth with lsk_ keys stored outside the repo and bound to least privilege scopes.

Fourth, commit project scoped configs and add a CI check that validates JSON schema and rejects new servers that request file write or network access without review. That keeps JetBrains AI Assistant aligned with your security policy and makes malicious config changes visible in pull requests.

```json
{
  "mcpServers": {
    "lyrashield": {
      "command": "npx",
      "args": ["-y", "@lyrashield/mcp"],
      "env": {
        "LYRASHIELD_API_KEY": "${LYRASHIELD_API_KEY}"
      },
      "disabled": false,
      "autoApprove": []
    },
    "docs-remote": {
      "type": "streamableHttp",
      "url": "https://app.lyrashieldai.com/api/mcp",
      "headers": {
        "Authorization": "Bearer ${LYRASHIELD_API_KEY}"
      }
    }
  }
}
```

Document every MCP server in a central inventory with purpose, trust level, and owner. Rotate keys monthly and log tool calls where your IDE allows it. Require two reviewers for any change to MCP config.

## Secrets, dependencies and prompt injection hardening

AI assistants accelerate copy paste of sample code that contains placeholder tokens or private URLs. Scan every AI diff for secrets before commit. Use gitleaks or cloud secret scanning and block merges on high confidence findings. CWE 798 and CWE 200 cover credential exposure and sensitive data leakage patterns that appear often in generated code [CWE 200 for sensitive data exposure](https://cwe.mitre.org/data/definitions/200.html).

Dependencies are the second common gap. AI often suggests outdated versions. Resolve versions with Open Source Vulnerabilities database [OSV.dev database](https://osv.dev/) and GitHub dependency review [GitHub dependency review](https://docs.github.com/en/code-security/supply-chain-security/understanding-your-software-supply-chain/about-dependency-review). Update lockfiles in the same PR and require explicit review of lockfile diffs, never auto approve even if the diff looks small.

Prompt injection can come from docs, issue comments, or web fetched content that the assistant brings into context. Add explicit agent instructions that require approval before write actions and never act on instructions embedded in README files. Restrict network egress for local MCP servers via OS firewall rules where possible, and log anomalous outbound calls that MCP tools trigger. For Node backends, apply Node security guidance for env isolation [Node.js security best practices](https://nodejs.org/en/learn/getting-started/security-best-practices).

Add these to your Definition of Done: secrets scan passes with no new findings, SCA passes with no high severity advisories, and a second engineer reviews any change that touches auth, sessions, payments, file uploads, or crypto. Keep a reproducible SBOM so you can trace each artifact back to its source.

## Verifying and gating AI generated code before merge

Vibe coding is fast, but velocity without verification creates security debt. Use a diff aware pipeline that only flags AI changed files. Keep human and AI commits separated where possible with a trailer or label, and map findings to OWASP Top 10 for consistent triage. Use one security gate table in your PR template to make checks explicit and auditable.

| Check | Where it runs | Owner | Fail action |
| --- | --- | --- | --- |
| MCP config schema validation | Pre commit hook | Developer | Block commit and explain fix |
| Secret scanning with gitleaks | CI diff scan | Security | Block merge until rotation confirmed |
| SCA via OSV and GitHub review | CI pull request | Developer | Block merge on high severity |
| SAST for OWASP Top 10 patterns | CI pull request | Developer | Require fix or documented exception |
| Prompt injection test cases | Manual QA checklist | QA | Request rewrite of prompt or context |

Export SARIF from each scanner and upload to GitHub code scanning so findings show inline on the PR [SARIF support in GitHub code scanning](https://docs.github.com/en/code-security/code-scanning/integrating-with-code-scanning/sarif-support-for-code-scanning). Keep reports for audit and always retest after fixes. Retest must run the same scanners on the fix commit to confirm remediation.

For frameworks like Next.js, verify server only secrets never leak to client bundles by checking env handling [Next.js env handling](https://nextjs.org/docs/app/api-reference/config/next-config-js/env). For Vercel hosted apps, enforce headers and WAF rules [Vercel security docs](https://vercel.com/docs/security). Enforce branch protection that blocks merge unless SARIF, SCA, and secrets checks report success, and keep evidence for compliance reviews.

## Runtime checks and observability after deploy

Checks do not stop at merge. Add runtime protections that catch what static analysis misses. Enable Content Security Policy, enforce strict CORS, and log auth failures centrally. Monitor for anomalous outbound calls from MCP tools that may have been compromised during development. Keep an inventory of which AI tools can make network calls and review it weekly.

Store build attestations so you can trace any production artifact back to a verified commit hash. Schedule quarterly exercises where you insert a known vulnerable snippet that AI might suggest and verify your gate catches it. Use OWASP WebGoat patterns as test payloads and track detection rate as a metric for your secure vibe coding program [OWASP WebGoat](https://owasp.org/www-project-webgoat/).

Supply chain hygiene matters because AI generated code often pulls transitive dependencies you did not explicitly choose. Lock files, provenance, and signed commits reduce risk that a compromised package enters your build. Pair that with branch protection that requires two reviewers for MCP config changes and for any change to auth middleware.

## Why pair JetBrains AI Assistant with LyraShield

LyraShield AI is an agent native application security platform for AI built software at [https://lyrashieldai.com](https://lyrashieldai.com) and our core loop is Target then Scan then Verified Finding then Fix PR then Retest then Report, designed for teams shipping with JetBrains AI Assistant and other AI coding tools.

Today our team has shipped Sprints 0 through 2 foundations. The full scan engine MVP, automated fix-PR generation, and billing are not live, so we describe them as designed to behave in specific ways and features on the roadmap rather than claiming live scans with remediation already shipped. What is designed to help JetBrains AI Assistant users includes a single MCP server you can add via stdio `npx -y @lyrashield/mcp` or remote [https://app.lyrashieldai.com/api/mcp](https://app.lyrashieldai.com/api/mcp) with Bearer lsk_ key, diff aware gating that posts SARIF to GitHub code scanning, and v1 coverage that is designed to include agentic pentest plus SCA plus secrets plus GitHub Action plus SARIF, with findings mapped to CWE and OWASP for faster remediation.

LyraShield is designed to sit after your JetBrains AI Assistant drafts and before merge, giving you verified findings instead of noisy rescans. It is on the roadmap to compare target behavior before and after an automated fix-PR and to enforce retest as part of the check suite.

Ready to apply this checklist beyond this tool. Run the interactive version at [AI app security checklist](/tools/ai-app-security-checklist) and bring your score to the waitlist at [https://lyrashieldai.com](https://lyrashieldai.com) to get early access to the MCP server and GitHub Action.
