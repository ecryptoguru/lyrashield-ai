# @lyrashield/cli

This is a scoped npm alias for the primary [`lyrashield`](../cli) CLI. It exposes the same `lyrashield` binary.

## Usage

```sh
npx @lyrashield/cli <command> [args]
```

For the canonical unscoped package:

```sh
npx lyrashield <command> [args]
```
