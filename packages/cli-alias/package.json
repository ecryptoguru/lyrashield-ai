{
  "name": "@lyrashield/cli",
  "version": "0.1.0",
  "description": "Deprecated alias for the LyraShield AI CLI. Use the `lyrashield` package instead; this alias will be removed in the next major release.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "bin": {
    "lyrashield-cli": "./bin/lyrashield-cli.mjs"
  },
  "files": [
    "bin",
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "security",
    "appsec",
    "ai-coding",
    "mcp",
    "lyrashield",
    "cli"
  ],
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "build": "tsup",
    "prepublishOnly": "tsup"
  },
  "dependencies": {
    "lyrashield": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsup": "^8.5.1",
    "typescript": "6.0.3"
  }
}
