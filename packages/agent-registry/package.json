{
  "name": "@lyrashield/agent-registry",
  "version": "0.0.0",
  "private": true,
  "description": "LyraShield AI coding-agent registry — the single source of truth for integrations, installers, docs and rules.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@iarna/toml": "^2.2.5",
    "@types/iarna__toml": "^2.0.5",
    "@types/node": "^22.20.1",
    "jsonc-parser": "^3.3.1",
    "typescript": "^6.0.0",
    "vitest": "^4.1.9",
    "yaml": "^2.7.1"
  }
}
