{
  "extends": "../config/tsconfig/library.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  // The v1-route probe imports a Next.js route file outside this project's
  // rootDir; vitest resolves it at run time, tsc must not.
  "exclude": ["node_modules", "dist", "src/__tests__/sarif-v1-route.test.ts"]
}
