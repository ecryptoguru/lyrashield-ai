# AGENTS.md — LyraShield AI orientation

Start here. This file is the current handoff and execution queue; `codebase.md` is the implementation map and `PRD.md` Part C is the product/backlog and release-readiness source of truth. Code and schema always beat documentation.

## Repository

LyraShield AI is the evidence-backed release-assurance layer for AI-built software. Its loop is **Target → Scan → Evidence State → Fix Proposal → Retest → Assurance Report**. Findings remain detected until independent verification evidence exists, clean deterministic retests are described as retest-confirmed, and PR execution is a guarded future step that requires a server-generated patch and exact approval binding.

- `apps/web` — Next.js 16 dashboard
- `apps/worker` — BullMQ scan worker
- `apps/agent` — approval-gated agent actions
- `apps/marketing` — Astro 7 / Cloudflare Workers marketing site
- `apps/marketing-motion` — deterministic Three.js assurance-world motion workspace (production tooling; the Astro site consumes rendered posters/clips)
- `packages/*` — auth, config, credentials, db, egress-proxy, integrations, logger, MCP, score, security, types, UI, agent-rules, cli-alias, plus the CLI/agent-install stack (`packages/cli`, `packages/agent-registry`, `packages/agent-plugin`, `packages/sdk`). `packages/credentials` is the single source of truth for `~/.lyrashield/credentials.json` — its location, env-over-file precedence, default API URL, and normalization — shared by the CLI and MCP server so the two cannot drift; both bundle it via `noExternal`, so it stays private.

Do not rename the `@lyrashield/*` package scope or `LYRASHIELD_*` variables without founder approval. Public copy uses **LyraShield AI**; the canonical marketing domain is **`lyrashieldai.com`**. Trademark clearance remains a founder/legal decision.

## Current verified state — 2026-08-09

- Sprints 0–7, the agent action layer, SCA/secrets scanning, URL scanning, reports, schedules, notifications, MCP, the GitHub diff gate, reliability/tenant-safety hardening, and the social distribution loop are merged on `main`. GPT-5.6 routing/caps merged in PR #53; Deep Review v3 remediation merged in PRs #54–#57; deletion attribution and legacy-report fallback landed in PR #59; the premium UI and Assurance Story release is PR #60; Deep Review v4 remediation merged in PR #79; per-control assurance landed in PR #108; per-request GPT-5.6 accounting and removal of cost UI landed in PR #109; the coordinated AI-pipeline hardening landed in app PR #113 and engine PR #7; fail-closed scan admission and queue recovery landed in PR #115.
- **LyraShield Score + public scorecards + referrals (spec Phases 0–2)** are implemented (PR #43 plus post-review fixes): pure versioned score engine in `packages/score`, immutable `ScoreSnapshot` per completed scan, frozen-allowlist public scorecards with supersession notices and OG images, RBAC- and audit-gated share create/revoke, new-account-gated referral attribution with idempotent dual-sided agent-minute rewards, and the Phase 0 waitlist referral ladder. Landmines: `buildScorecardPayload` in `packages/db/src/score-service.ts` is the ONLY place a public payload may be constructed (its allowlist regression test is load-bearing); never add fields to it casually, and never derive share-eligibility client-side. See `codebase.md` §33.
- **Social distribution loop** is merged in PR #52: dynamic per-scorecard Open Graph/Twitter metadata; deterministic grade/fix cards in 1200×630, 1080×1080, and 1080×1350 formats; native/LinkedIn/X/Bluesky/WhatsApp/Reddit/email/copy/download sharing; revocable README badges; public conversion CTA; channel-preserving referral attribution; deduplicated privacy-safe view/share events; dashboard funnel counts; waitlist sharing/position; and client-handoff report copy. Public scorecard analytics never store target, repository, finding, IP, user-agent, or caption data. See `codebase.md` §34.
- **Premium UI and Assurance Story reports (PR #60)**: the dashboard, auth, public scorecards, shared reports, and Astro marketing site use one responsive light/dark design system; the mobile app shell uses accessible Sheet navigation; the command center renders score, severity, trend, and remediation visuals; and executive/developer/compliance reports render immutable creation-time snapshots with privacy-redacted public sharing. See `codebase.md` §37.
- Auth and routing review completed: sign-up/sign-in now handle email verification, redirect via `callbackURL`, and avoid `useSession` atom issues in `apps/web/src/app/sign-in/page.tsx` and `apps/web/src/app/sign-up/page.tsx`.
- **Evidence-backed release assurance marketing (PR #69):** the public site now leads with `Target → Scan → Evidence State → Fix Proposal → Retest → Assurance Report`, makes detected, independently verified, retest-confirmed, and inconclusive states explicit, links a public methodology page, and routes five browser-local no-upload tools into open registration. Fix PR execution remains fail-closed; no page claims universal verification, automatic PR execution, security guarantees, production coverage, pricing, benchmarks, customers, or the upstream engine.
- **Production marketing SEO/AEO/GEO and Lite Scanner foundation (PR #71; security-gate follow-up PR #72):** the indexable public surface has apex canonicals, structured data, robots/sitemap/`llms.txt`, richer methodology/sample-report/tools content, page-scoped indexing, a disabled-until-configured scanner funnel, and deterministic Lite Scanner APIs plus signed privacy-bounded scorecards. Realistic scanner test fixtures are assembled from inert fragments so repository secret gates stay meaningful without weakening detection coverage.
- **Cloudflare marketing delivery hardening (PR #74):** mobile landing pages have native accessible navigation; Worker-generated APIs receive the defensive security, no-store, and noindex policy; waitlist bodies are streamed with a 16 KiB limit; and main-branch marketing changes deploy only after the repository security/test/build gates, remote D1 migrations, and live smoke checks. The first automated production deployment completed as Worker version `eba63368-9dd4-4dcb-bb0c-09f46c26ec7f`; PR #79 main run `29487616647` later deployed privacy-bounded pageviews and the v4 marketing fixes as version `31514039-473b-4837-95cf-d61da009e238`. GitHub uses a one-year account-owned Cloudflare token scoped only to Workers scripts, KV, D1, account read, and Workers routes.
- **Production Lite Scanner launch (PR #76):** the passive public scanner is live at `https://scanner.lyrashieldai.com` on Azure Container Apps behind managed TLS, with Supabase Postgres, Upstash Redis, Cloudflare Turnstile, origin-scoped CORS, a monitored `abuse@lyrashieldai.com` Cloudflare Email Routing address, and a separate `PUBLIC_SCANNER_URL` boundary. Production health/readiness, Turnstile, CORS, fail-closed bot checks, all five browser-local tools, waitlist validation/no-write honeypot behavior, and a real browser Lite Check against the canonical marketing site passed. This launch does not expose the full worker/engine scan pipeline.
- **Homepage Lite Check funnel and breadcrumb cleanup (PR #84):** the homepage primary action now opens a responsive passive Lite Check form, validates public HTTP(S) targets, records authorization and Terms acceptance, and hands the target to the existing Turnstile/results route through session storage without placing it in the URL. Visible breadcrumb rows are removed across marketing page families while structured breadcrumb metadata remains. Header/footer Free scan links target the homepage form; `/scan` remains the canonical detailed scanner route.
- **Scan progress and future-product preview (PR #85):** Lite Check requests render an accessible animated public-surface map and cycle through truthful review categories without inventing backend completion percentages. The homepage assurance section now shows clearly labeled illustrative evidence states alongside the real Vibe Security 50 split of 43 machine-testable and 7 evidence-required controls; reduced-motion and mobile layouts are covered by regression and rendered QA.
- **Direct scan URLs and one-pass progress (PR #86):** the homepage and `/scan` accept bare domains such as `lyrashieldai.com`, normalize them to HTTPS before hashing or scanning, and run the five activity rows exactly once. Fast results wait for the four-second pass; slow results hold on the fifth row without looping. Reduced-motion users still receive the result without an artificial delay.
- Current `main` passes **1,482 core tests in 146 files** (8 skipped), **112 marketing tests in 15 files**, **16 motion tests**, and **3 Chromium E2E tests**, plus lint, typecheck, production build, formatting, Prisma client generation, migration drift/application across **31 migrations**, SCA/secret scanning, and `git diff --check`. Local Docker Compose now builds `web`, `migrate`, and `worker` images and starts a healthy full stack (Postgres, Redis, web, worker) with `/api/health` returning `ok` and the worker processing scan jobs.
- **Redis architecture separation (2026-07-29):** `REDIS_URL` (redis://) is reserved for the BullMQ job queue and connects to a dedicated Redis instance — local Docker Redis in development, Azure VM-hosted Redis in production. `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` (HTTPS REST) are exclusively for distributed rate limiting via `@upstash/ratelimit`. The two are never interchangeable: conflating them silently broke production rate limiting. Docker Compose no longer passes Upstash env vars to web/worker services (rate limiting falls back to in-memory in dev). Production Azure Container Apps set both `REDIS_URL` (Azure VM Redis) and `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` (Upstash free plan) as separate secrets/env vars. The worker VM retrieves `REDIS_URL` from Key Vault via `refresh-secrets.sh`.
- **Deploy workflow hardening (2026-07-29):** the Azure deployment GitHub Actions workflow now retries app and scanner health smoke checks for up to 10 minutes (60 attempts × 10s) instead of a fixed 30-second sleep, and the worker image digest step correctly gates on `steps.build-worker.outcome` rather than checking for engine files that may not exist in the runner context.
- The engine is a controlled LyraShield derivative of Strix, not a thin wrapper. Engine PRs #6, #7, and #20 added bounded context compaction, the GPT-5.6-only execution/evidence contract, and rate/telemetry/accounting fixes while retaining a pinned upstream substrate, telemetry-off default, and approval-gated sync. Current engine `main` modifies 68 upstream source files (+5,397 / −1,297 lines under `strix/**` against pinned base `2e7040240d`) and passes **597 tests (1 skipped)** plus Ruff, formatting, headless mypy, Bandit, native-binary checks, sandbox build/smoke, and worker compatibility. The adapter (`lyrashield_adapter/cli.py`) is the public entry point, but significant product logic — cost accounting, compaction, lifecycle, finding identity, evidence, and the worker contract — lives intentionally inside modified `strix/**` modules; see the engine repo's `UPGRADES.md` for the ownership ledger. Keep sandbox/tool/SDK plumbing upstream-derived; LyraShield owns model policy, budgets, compaction, lifecycle, finding identity, evidence, and the worker contract. Do not expand the fork or claim result improvements without a LyraShield evaluation corpus.
- The worker image builds the sibling engine source, exposes the CLI on `PATH`, includes Git for repository targets, and fails before sandbox setup when model configuration is missing. One pre-v4 local Safe scan against an approved public repository completed through the Docker sandbox with Luna/medium routing and recorded a post-run budget-overage warning under the behavior that PR #79 has since replaced with terminal/clamped handling. It found no issues; that result is scoped to that target and is not production, coverage, or security proof.
- **First approved production Standard scan completed (2026-07-29):** a Standard (Code Review) scan against `ecryptoguru/OnboardingAI2` completed successfully on the production Azure Container Apps stack using `azure_ai/gpt-5.6-luna` at medium reasoning. Duration: 6m 53s; billed cost: $1.78 (96 LLM requests, 4.9M input tokens with 74% cache hits, 22K output tokens); 40 findings (2 CRITICAL, 2 HIGH, 36 MEDIUM) with 0 independently verified; tamper-evident manifest saved (SHA-256 `bc233165…`). Coverage: 1 finding mapped, 9 no-finding, 7 evidence-required, 27 inconclusive, 6 not-applicable across the Vibe Security 50. This is a production Standard/Luna scan against an approved target; it is not a Deep/Terra scan, not a security guarantee, and not coverage proof. A prior attempt on the same target failed because two Prisma migrations (`20260725132208_add_finding_status_reason`, `20260725160000_scan_workspace_status_index`) had not been applied to the production Supabase database; both were applied manually via the Supabase MCP and registered in `_prisma_migrations`.
- **Azure AI / GPT 5.6 mode routing and protected limits are implemented**: Safe/Quick/Standard scans select `LYRASHIELD_LUNA_LLM` at `medium` reasoning; Deep/Custom use a `LYRASHIELD_TERRA_LLM` coordinator at `medium` with `LYRASHIELD_LUNA_LLM` specialists at `high`. `LYRASHIELD_LLM` remains the backward-compatible fallback. Default engine caps are $3.20, $1.20, $3.20, $15, and $15 respectively; a positive workspace `Policy.maxBudgetUsd` overrides the mode default. The worker retains private usage telemetry, separates per-request standard and long-context buckets, applies the official GPT-5.6 Terra/Luna rate card and cache-write rates, and never derives a precise amount from an ambiguous aggregate. An overage terminates as `STOPPED_BUDGET` and persisted billable cents are clamped to the approved cap. Azure credentials and endpoints remain shared through `AZURE_AI_*`, `AZURE_OPENAI_*`, or generic `LLM_*` variables. On a root content-filter block, the engine switches directly to Luna/high without retrying Terra; if the delegate also blocks, the scan salvages partial findings and terminates with `content_filter_stopped`.
- **AI-pipeline hardening (app PR #113; engine PR #7):** execution accepts GPT-5.6 Terra or Luna deployments only; the inherited Perplexity integration and other non-OpenAI model-provider credentials remain outside the worker boundary. Pre-request reservations cap output, agents, and concurrent spend; deterministic identities preserve distinct code/package findings and corroborating receipts; explicit control IDs own coverage; worker usage is checkpointed before downstream scanners; raw engine output is not persisted; and evidence/manifests retain bounded reproducibility metadata. Non-interactive execution avoids Rich rendering and target-derived error output, host-gateway access is opt-in, and worker/engine cleanup is restricted to validated owned paths. See `codebase.md` §52.
- **Parallel Search `web_search` integration (2026-08-04):** the engine now exposes an optional `web_search` tool backed by Parallel Search when `LYRASHIELD_WEB_SEARCH_ENABLED=1` and `LYRASHIELD_WEB_SEARCH_API_KEY` is present. The tool is available to all scan modes while per-mode gating is evaluated; the engine redacts target hosts, secrets, IPs, and PII before sending a query and tracks calls against `LYRASHIELD_WEB_SEARCH_BUDGET_USD` and `LYRASHIELD_WEB_SEARCH_MAX_CALLS_PER_SCAN`. The worker's `buildEngineEnv()` allowlist, `packages/config`, `.env.example`, `docker-compose.yml`, and production `ops/worker` scripts have been updated to pass the `LYRASHIELD_WEB_SEARCH_*` variables; `ops/worker/refresh-egress.sh` adds `api.parallel.ai:443`.
- **Fail-closed scan queue recovery (PR #115):** worker replicas register Redis heartbeats only after BullMQ is ready; every manual, retest, schedule, and agent enqueue path refuses admission with `SCAN_SERVICE_UNAVAILABLE` when no worker is live. Startup/minute reconciliation uses a renewable token-owned lease, fails five-minute queue orphans without replaying paid work, removes non-active jobs whose scan is missing or terminal, and contains BullMQ failure callbacks. `/api/ready/scans` reports scan-service readiness separately from web readiness; Docker uses a worker-local heartbeat marker. The 17 confirmed E2E fixtures were reconciled without touching unrelated data, E2E no longer creates real queue jobs, and active queue/database state was verified empty before the rebuilt worker started. See `codebase.md` §53.
- **Assurance controls and private accounting (PRs #108 and #109):** the dashboard exposes Release Check, Code Review, and Deep Security Review plus Weekly Monitor schedules while showing no model cost, spend, cap, or accounting events. Scan details retain user-relevant model/reasoning, immutable manifests, per-control receipts, findings, and the Vibe Security 50 ledger. Accounting remains internal for operator reconciliation. The complete end-user workflow and every current option are documented in `userguide.md`.
- The marketing Worker is deployed and indexable at **`https://lyrashieldai.com`** on Cloudflare with custom apex and `www` domains, an active path/query-preserving 301 from `www` to apex, production D1 plus all three migrations, Cloudflare Rate Limits, a generated `WAITLIST_IP_SALT` Worker secret, KV sessions, HTTPS, defensive response headers, canonical metadata, sitemap/robots, and an AEO/GEO `llms.txt`. Post-PR-#74 live verification covered 10 sitemap pages and 23 discovered internal URLs with zero broken links or metadata/schema issues; the homepage scored 91/100/100/100 mobile and 94/100/100/100 desktop for performance/accessibility/best-practices/SEO with zero console errors. PR #76 made the separately hosted Lite Scanner available and indexable; legal terms remain individually `noindex`. Scanner controls still fail closed without the scanner origin and never fall back to localhost. See `apps/marketing/README.md`.
- **Vibe Security 50 and free tools (this release):** the versioned registry delivers 43 machine-testable controls to scans and separates the remaining 7 evidence-required controls. `/tools` provides five browser-local utilities (launch checklist, headers/CORS, secret exposure, Supabase RLS, and JWT/session inspection); inputs are not uploaded. See `docs/vibe-security-50.md` and the dated SEO/tools plan. Do not claim all 50 are found or fixed.
- **Engine PR #20 merged:** the engine `main` now includes the `reduce-root-context` branch along with GPT-5.6 rate fixes, provider-reported cache-read accounting, deterministic test environment isolation, and Makefile/CI alignment. Subsequent engine PRs (#22, #26, #33, #35, #36, #39, #40) further hardened the GPT-5.6 execution boundary, Azure Foundry compatibility, cost/cache accounting, and run.json progress fields. The engine passes **597 tests (1 skipped)** plus ruff, mypy, bandit, native-binary, sandbox, and worker-compatibility checks.
- **Finding statusReason:** the dashboard and API now persist an optional user-provided reason when a finding is marked _Accepted risk_ or _False positive_. The reason is stored on `Finding.statusReason`, returned by the detail API, and rendered in the finding drawer. Unit tests cover the DB service path.
- **Agent Plugin integration (merged to `main` in PR #247):** a new `@lyrashield/agent-plugin` package implements the open Agent Plugins v1.0.0 spec (vendor-neutral standard from agent-plugins.org), packaging the MCP server and skills into a portable plugin with `plugin.json`, `mcp.json`, and `skills/lyrashield/SKILL.md`, plus client-specific manifest shims for Claude, Cursor, Codex, and Kiro. `packages/mcp` now resolves `LYRASHIELD_API_KEY`/`LYRASHIELD_API_URL` from env vars or falls back to `~/.lyrashield/credentials.json` (CLI credentials file, 0o600 perms). `packages/agent-registry` added an `"agent-plugin"` install strategy and a `pluginLocations` field for the 6 launch clients (Claude Code, Cursor, Windsurf, VS Code, Codex, Kiro). `packages/cli` added `agent-plugin` installer/uninstaller commands and `init` now prefers plugin installs when the agent supports it. `apps/web` `AgentCardData` type updated to include the `"agent-plugin"` install strategy. The plugin license is Apache-2.0; the hosted service remains proprietary.

### Blog batches 7–10, the `/compare` collection, offline CI gates, and the homepage rewrite (2026-08-07 to 2026-08-08, 13 PRs merged, `main` at `476fcc0`)

- **The blog program is 161 published articles, not the 100 from PR #88** (see below). Batches 7–10 were merged as five independent tranches so every merge to `main` was a complete, self-consistent release: PR #227 (batch-7), PR #230 + PR #235 (batch-8, split across two tranches because each new tool's shared hero image needs both its checklist and workflow post in the same commit), PR #233 (batch-9, the workflow companions for the tools already covered by batches `authority`–`batch-6`), and PR #234 (batch-10, 13 comparison-vs-competitor posts). `PROGRAM_ARTICLE_COUNT` (now `161`) and `IMAGE_CORPUS` (now `{ authority: 1, shared: 74 }`, 75 images total) are declared once in `apps/marketing/scripts/blog-validation-lib.mjs`, not hardcoded per script. The original exact `29×3 + 6×2` image-reuse histogram no longer holds at this size; the per-commit invariant is that declared `usageCount` equals actual usage and every image's usage stays inside `1..MAX_SHARED_IMAGE_USAGE`.
- **A parallel content collection, `/compare`, was built (PR #229) and populated (PR #234):** 13 competitor-comparison pages under `src/content/compare/*.md`, governed by `src/content/compare-program.json` and gated by `compare:validate`, the same shape as the blog program and sharing its `PROHIBITED_CLAIMS`/`PLACEHOLDERS` rules. Each page carries `TechArticle`/`BreadcrumbList`/`FAQPage` JSON-LD and links bidirectionally to its comparison blog post.
- **Two new CI gates (`blog:validate:mdx`, PR #228; `blog:validate:offline`, PR #236) close real gaps** the single-validator setup missed: an unterminated inline-code span letting `${VAR}` leak into a live MDX expression (compiles fine, fails only at prerender with no file name), and a maintained `DEAD_URLS` denylist plus an unquoted-YAML-scalar check (Astro's real `js-yaml` parser rejects a bare `description: "...: ..."` that the hand-rolled `parseArticle` accepted). Both run in CI on every commit alongside `blog:validate`, `blog:validate:images`, and `compare:validate`. `blog:check-links` (~50 live third-party URLs) is deliberately kept out of the CI gate set — a transient third-party timeout during this program is the concrete reason — and stays a manual sweep.
- **Publishing is confirmed tranche-merge, not draft-flag scheduling:** `draft: true` on a program-mapped article fails the release check, breaks the program-completeness test, and turns any published post's link into it into an `unpublished internal dependency` failure. `pubDate` has zero publish-gating logic anywhere in the codebase — it is sort order and RSS metadata only. Each merge to `main` is a complete, independently green release; the merge calendar is the publish calendar.
- **Homepage rewrite (PR #238):** new H1 `"Ship AI-built apps with evidence, not hope."`, dual CTAs (Lite Check anchored to the on-page `#free-scan` form, account creation to `app.lyrashieldai.com/sign-up`), honest open-beta/roadmap framing. The seven-chapter `lib/motion-manifest.ts` copy (chapter `id`s, and therefore the R2 motion media paths, unchanged) was reworded in the same pass. A `max-width: 480px`-scoped fix keeps the primary CTA above the fold at 390px, verified by rendering the actual `astro build` output in headless Chromium, not asserted; desktop is untouched outside the new media query.
- **A transitive `nanoid` high-severity advisory (GHSA-2v37-7h3g-55p8) was cleared (PR #240)** via `pnpm-workspace.yaml`'s `overrides` (`nanoid: ">=3.3.17"`) — `package.json`'s `pnpm.overrides` field is silently ignored by pnpm 11.
- **Not yet done:** no scheduled workflow runs `blog:check-links`; a newly-dead external URL is only caught by a manual sweep or by already being in `DEAD_URLS`.

### OAuth/legal routes, MCP endpoints, and marketplace v0.1.8 (PR #247, merged 2026-08-09, `main` at `dae5153`)

- **Hosted OAuth for MCP connections is merged.** Remote MCP clients (`/api/mcp`) now authenticate via hosted OAuth 2.0 with workspace selection and optional write scope, in addition to the existing `lsk_` API-key bearer flow. New routes: `/oauth/consent`, `/oauth/select-workspace`, `/device`, `/device/approve`, `.well-known/oauth-authorization-server`, `.well-known/oauth-protected-resource` (both at root and under `/api/mcp/`). The OAuth provider is persisted in the database via migration `20260808000000_oauth_provider` and the `OAuthClient`/`OAuthConsent`/`OAuthToken` models in `schema.prisma`. Remote connections are read-only by default; write actions require explicit scope and approval. OAuth clients can never use the `LYRASHIELD_MCP_ALLOW_REMOTE_MUTATIONS` bypass.
- **CLI OAuth device login is merged.** `packages/cli` added a `login` command that opens a browser-based device approval flow, writes tokens to `~/.lyrashield/credentials.json` (0o600), and falls back to `LYRASHIELD_API_KEY` from the environment. `packages/credentials` is the single source of truth for that file — its location, env-over-file precedence, default API URL, and normalization — shared by the CLI and MCP server so the two cannot drift.
- **Marketplace v0.1.8 is tagged and released** at `ecryptoguru/lyrashield-marketplace` (tag `v0.1.8`, release https://github.com/ecryptoguru/lyrashield-marketplace/releases/tag/v0.1.8). The export reconciles the drifted v0.1.7 with current source, including the corrected Codebuff publisher slug (`lyrashield`, not `lyrashield/lyrashield-review@0.1.0`), refreshed OAuth/legal link metadata, and the Apache-2.0 plugin license. The export is generated by `pnpm --filter @lyrashield/agent-plugin export:marketplace <path>`; `manifest.json` records the source package, version, generated files, and forbidden hosted-service paths. The export test fails if an artifact disappears.
- **Legal/support pages are merged.** `/privacy`, `/support`, and `/security-reporting` are live on the marketing site (`apps/marketing/src/pages/*.astro`).
- **Brevo email integration is verified locally.** `LYRASHIELD_REQUIRE_EMAIL_VERIFICATION=1` with `BREVO_API_KEY`, `EMAIL_FROM`, and `NOTIFICATION_FROM_EMAIL` set boots cleanly in `NODE_ENV=production`; a live test email was sent and accepted by Brevo. Brevo IP security is disabled at the account level because Azure Container Apps Consumption has 180+ dynamic outbound NAT IPs that cannot be statically allowlisted. Production Container App secrets for `BREVO_API_KEY`, `EMAIL_FROM`, and `NOTIFICATION_FROM_EMAIL` are not yet provisioned — the production app still runs with `LYRASHIELD_REQUIRE_EMAIL_VERIFICATION=0`.
- **Full test suite passes:** 1,482 core tests (8 skipped), 112 marketing tests, 16 motion tests, lint, typecheck, format check, and `apps/web` production build all green.

### Working changes (unmerged, 2026-07-31)

- **CLI path and exit-code contract:** all CLI command handlers now pass bare API paths (e.g., `/findings`) to the SDK; `packages/sdk/src/client.ts` `buildUrl()` continues to prepend `/api/v1`. Top-level error handling maps HTTP statuses to exit codes `3` (401/403), `4` (other API/network failures), and `5` (429). `packages/cli/src/output.ts` was updated so JSON-mode `error()` exits with the command's chosen code instead of always `1`. `packages/cli/README.md` documents the new commands and exit codes.
- **`fix-plan` CLI split:** `lyrashield fix-plan <findingId>` is now read-only and returns `recommendedFix` and `plainLanguage`. Creating a fix proposal moved to `lyrashield fix-plan create <findingId> --summary <summary>` with the same 10-character minimum the API enforces.
- **Agent-config scanner hardening:** `apps/worker/src/engine/scanners/agent-config-scanner.ts` removed the `lyrashield:begin/end` managed-block checksum bypass entirely. Every instruction file is now scanned as plain text, so a correctly-checksummed block can no longer hide malicious instructions. `packages/agent-rules/src/__tests__/scan.test.ts` asserts both wrong and correct checksums still flag the text.
- **Agent registry path corrections:** `packages/agent-registry/src/agents.ts` now uses platform-specific global VS Code settings paths (`~/.config/Code/User/settings.json`, `~/Library/Application Support/Code/User/settings.json`, `~/AppData/Roaming/Code/User/settings.json`) and the concrete Zed global path (`~/.config/zed/settings.json`); `packages/cli/src/installers/detect.ts` `resolveLocation()` already consumes `loc.platform`.
- **Secret-mode installer refactor:** `packages/cli/src/installers/secret-mode.ts` now treats `inline` and `header` modes as raw-secret writes and refuses to write them into `sharedByConvention` files unless `--inline-secret` is passed and the file is gitignored. The unused `secretValue()` helper was removed.
- **Rate-limit and approval controls:** `apps/web/src/lib/rate-limit.ts` added `checkApprovalCreateRateLimit` (10 approvals per workspace per minute). `apps/web/src/app/api/agent-approvals/route.ts` calls it after `requirePermission()`. A new untracked `apps/web/src/middleware.ts` applies the global `checkApiRateLimit` (30/min per IP) to all `/api/:path*`.
- **Approval default expiry:** `packages/db/src/agent-approval-service.ts` default `expiresAt` tightened from 24 hours to 15 minutes, matching the route default.
- **MCP path normalization:** `packages/mcp/src/tools.ts` `apiCall()` now strips `/api/v1` and `/api` prefixes before passing paths to the SDK client, fixing double-prefixing when tools call `/api/v1/...`.
- **Remote-HTTP vs stdio URL contract:** `packages/agent-registry/src/render.ts` `deriveMcpUrl()` resolves the remote-HTTP `url`/`serverUrl` to `<apiUrl>/api/mcp` while the stdio `LYRASHIELD_API_URL` env block uses the base `apiUrl`. `apps/marketing/src/components/AgentSnippet.astro` now uses the base URL for manual/vendor commands and `/api/mcp` for remote-HTTP snippets.
- **Turbo task graph:** `turbo.json` adds a `test` task with `dependsOn: ["^build", "generate"]` and `cache: false`, and adds `generate` to the `typecheck` task dependencies so Prisma client generation runs before test/typecheck.

#### Deep Review v11 remediation (unmerged branch `codex/deep-review-v11`, 2026-08-01)

- **Batch A (stop-bleeding P0s):** CLI install/agent fixes.
- **Batch B (structural tenancy):** `workspaceId` enforcement added to `fix-proposal-service`, `retest-service`, `report-service`, `agent-approval-service`, and `schedule-service`; Prisma migration `20260803000001_child_table_rls` defined fail-closed RLS policies for `ScanEvent`, `Evidence`, `ScorecardShare`, `ScorecardEvent`, `ReferralCode`, `ReferralAttribution`, `NotificationPreference`, and `OnboardingState`, and `20260803000002_child_table_rls_enable` added the missing `ENABLE ROW LEVEL SECURITY`. `20260803000003_child_table_rls_disable` then disabled RLS on these nine child tables after a production `42501` outage, and `20260807000003_child_table_rls_re_enable` **restored it once the root cause was found and fixed**: `account-deletion.ts` was writing to `ScorecardShare` outside `withWorkspaceRLS`, so the policy's `WITH CHECK` correctly rejected the write. **Database-level tenant isolation on these nine tables is ON in production as of 2026-08-07.** The fail-closed tests are un-skipped and now include a same-workspace _write_ assertion — read-only assertions cannot distinguish "fails closed correctly" from "always fails", which is why the original gap reached production — and a dedicated `rls-child-write-repro` CI job exercises them against real Postgres under a restricted non-superuser role on every run. See Deep Review v12 P0-1. `score-service.ts` and `run-scan.job.ts` were updated to pass `workspaceId` and use `withWorkspaceRLS` where required.
- **Batch C (worker recovery):** `command-builder.ts`, `finding-persister.ts`, `result-integrity.ts`, `scanner-orchestrator.ts`, `run-scan.job.ts`, and `apps/worker/src/index.ts` hardened worker recovery, result integrity, and engine command building.
- **Batch D (build/deploy integrity):** the OpenAPI spec builder moved from `apps/web/src/lib/openapi/` to a declared `@lyrashield/types/openapi` export, eliminating a cross-app import from the marketing app; the marketing app now depends on `@lyrashield/types`. `deploy-azure.yml` pins all Docker actions to release SHAs, emits `web_digest`/`worker_digest`, deploys app/scanner images as `image:tag@sha256:<digest>`, and splits worker image cleanup to `keep-n-tagged: 100` so a VM-pinned worker digest is not garbage-collected.
- **Batch E (polish/test regression fixes):** `packages/cli/src/installers/atomic-write.ts` no longer rejects paths under a symlinked ancestor such as macOS `/var`; it only refuses when the immediate destination directory is a symlink. Unit tests were added/updated for `saveApprovalResult` workspace scoping, `evidence.createMany`, and the `atomic-write` symlink guard.
- **Verification:** lint, typecheck, `git diff --check`, and package-level unit tests pass. Full `pnpm test` is at **1337 passed / 10 skipped / 3 failed**, with the failures limited to DB integration suites (`account-deletion`, `audit-concurrency`, `soft-delete`) that require a live Postgres/Redis stack. The `python3 .devin/scripts/checklist.py .` required checks (Security, Lint, Schema) pass.

### Dashboard UX final pass (unmerged branch `dashboard-ux-final-pass`, 2026-08-03)

- A shared `PageHeader` component (`apps/web/src/components/page-header.tsx`) now provides consistent title, icon, description, and action layout across dashboard list and detail pages.
- `NoWorkspaceState` (`apps/web/src/components/no-workspace-state.tsx`) replaces ad-hoc empty-state copy when a user has not yet created a workspace on the dashboard pages that need one.
- The affected server and client pages (`approvals`, `fixes`, `launch-readiness`, `notifications`, `projects`, `scans`, `scans/[id]`, `settings`, `targets`, `team`) were updated to use `PageHeader` and the new no-workspace placeholder where applicable.
- Prettier formatting was applied to `page-header.tsx`.

### UX V2 merge (Phases 0–10, merged on `main`)

- UX V2 Phases 0–10 are merged on `main`. The dashboard now has a mobile-first shell (`V2Sidebar`, `BottomNav`, `MobilePageHeader`), a feature-flags system with env and per-workspace cookie override, a PostHog product analytics wrapper with privacy-safe allowlist, and a terminology mapping module (`apps/web/src/lib/terminology.ts`) that maps internal identifiers to user-facing labels (Scan→Trust Run, Finding→Issue, Project→Product, Target→Asset).
- New schema additions via migration `20260803000000_uxv2_schema`: `Project.trustPlan`, `NotificationPreference`, `Scan.durationMs`. The `NotificationPreference.updatedAt` field is aligned with its migration via `@default(now())`.
- New V2 route aliases (not renames): `/dashboard/products` (targets), `/dashboard/runs` (scans), `/dashboard/issues` (findings), `/dashboard/approvals`, `/dashboard/evidence` (reports), `/dashboard/automations` (schedules). Legacy URLs remain canonical with `next.config` rewrites or 308 redirects.
- The dashboard home renders a `TrustCommandCenter` using workspace project trust-plan data, completed scan count, and latest score. Evidence and Approvals pages are functional. Notification preferences UI is live. A share sheet with privacy-safe channels is implemented.
- The worker schema accepts engine `run.json` progress fields (`phase`, `seq`, `turn_count`) via `engineRunRecordSchema`.
- Onboarding state creation is race-free via raw `INSERT ... ON CONFLICT DO NOTHING` in `apps/web/src/lib/onboarding-state.ts`, replacing the Prisma `upsert` that could surface P2002/23505 under concurrent server-component and API calls.
- Playwright visual baseline snapshots exist for dashboard, products, runs, issues, and onboarding at desktop, iPad, and iPhone viewports. Visual E2E tests are skipped in CI (`--grep-invert '@visual'`) until Linux baselines are generated; the 3 Chromium E2E critical-flow tests remain in CI.
- The marketing homepage includes a V2 launch highlight section.

### Premium assurance world and authority blog release (PR #88, merged and deployed)

- PR #88 merged on 2026-07-17 after green CI and deployed the premium assurance world plus exactly 100 published authority-blog articles and a 36-image source-artwork library. The canonical homepage now uses the seven-chapter evidence world; `/premium-preview` was removed.
- Immutable motion version `14cdcb1b53692d73` is served from the `lyrashield-marketing-media` R2 bucket at `https://media.lyrashieldai.com`. The custom domain uses TLS 1.2+, exact-origin `GET`/`HEAD` CORS for `https://lyrashieldai.com`, byte ranges, correct media MIME types, and one-year immutable caching.
- **Production motion hardening (PR #91, merged and deployed):** the evidence-world controller now coalesces seeks, never writes `currentTime` while a seek is active, reveals only decoded frames, keeps exclusive foreground-video ownership, retains only the current and next chapter blobs, and ignores height-only mobile viewport changes. Active copy is centered inside the pinned world on desktop and portrait; reduced-motion, Save-Data, media-failure, and no-JavaScript paths remain poster/readable-content fallbacks. Existing HyperFrames/Three.js render `14cdcb1b53692d73` was retained because the immutable media passed delivery, dimension, decode, and range checks; no Higgsfield is used.
- `apps/marketing/src/content/blog-program.json` owns program membership. Batch validators, the exact completeness test, and `apps/marketing/scripts/crawl-built-blog.mjs` provide the release gate for content contracts, images, links, built routes, canonicals, headings, landmarks, anchors, JSON-LD, RSS, and tag archives. Crawler reports must remain query-free.
- The exact completeness test required all 100 local entries published at the time of this release; the program has since grown past 100 (see the dated section above for the current count and gate). New articles remain drafts by default until explicitly approved.
- Local validation, Worker-backed browser QA, green CI, guarded deployment, and live homepage/media/sitemap checks passed for this release. New articles remain drafts by default until explicitly approved.

### Recent hardening and infrastructure merge

- **Engine fallback and scan labeling hardening (2026-08-04, engine `fix/broaden-delegate-fallback` + app `main`):** the engine now falls back from the root Terra agent to the Luna delegate on ANY `ModelBehaviorError` (not just `content_filter`), salvages partial findings on any delegate error with a new `engine_stopped` terminal reason, and treats Azure's `response.failed` status (without content-filter context) as a transient error retried with backoff. The worker's `run-scan.job.ts` classifies `engine_stopped` scans with findings as `COMPLETED` (error category `ENGINE_STOPPED`) rather than `FAILED`, preserving partial results for the user. Exit code mapping: 2 (findings present) or 5 (no findings). E2E verified: a DEEP scan completed with 14 engine findings, 167 LLM requests, 9.8 min duration, exit code 2, correct `COMPLETED` + `ENGINE_STOPPED` status. See `codebase.md` §54.

- **Engine CI quality gates (2026-08-04, engine `fix/broaden-delegate-fallback`):** the engine CI workflow now runs ruff lint/format check, mypy type check, bandit security scan, and the full pytest suite (597 tests) — previously only ran thin-fork verification, CLI build, and worker contract checks. This closes the gap where `--no-verify` could bypass pre-commit hooks. Dependabot now also tracks Python pip dependencies (was GitHub Actions only).

- **Dockerfile HEALTHCHECK and compose resource limits (2026-08-04, app `main`):** the runner (web) and worker Dockerfile stages now include `HEALTHCHECK` instructions for orchestrator-native health monitoring. The web service in `docker-compose.yml` now has CPU/memory resource limits (worker already had them).

- **Deep Review v4 remediation (PR #79):** path-based SCA/secrets/agent-config findings are no longer suppressed by URL false-positive heuristics; sequential referral-owner deletion uses unique per-row sentinels; provider overages stop and clamp billing; shutdown terminates active engine processes; findings pagination, report/scan state, loading UX, accessibility semantics, preview RSS gating, shared client-IP parsing, waitlist-position rate limits, worker resource limits, and evidence retry deduplication are corrected. Forward database constraints enforce canonical GitHub installation IDs and one active share per score snapshot; scorecard publishing is cross-admin idempotent; approval mutations use typed errors. Marketing emits query-free `$pageview` events to PostHog, and `lyrashieldai.com` is an authorized analytics domain. See `codebase.md` §45.

- **Security remediation (PR #66):** all 14 findings from the full code review are merged. URL targets never enter the unpinned external engine; engine artifacts and output fields are bounded and schema-filtered; scanner cancellation reaches SCA, secrets, agent-config, URL, and OSV work; `fec0::/10`, credential-bearing URLs, query strings, and fragments are rejected; diagnostic URLs are redacted. Engine-originated findings are never self-verified. `CUSTOM` agent scans require approval alongside `DEEP` scans. Personal notification feeds return only the caller's rows plus workspace-wide rows. GitHub installation IDs have a global uniqueness migration, and fresh callback claims fail closed until provider ownership proof exists. Fix-PR creation accepts no client patch data and fails closed until a server-generated, approval-bound patch pipeline exists. See `codebase.md` §39.

- **Result integrity (PRs #67 and #68):** new scans persist an immutable result manifest, scanner coverage receipts, privacy-bounded finding candidates, and idempotent detection/verification receipts. Confidence never sets `Finding.verified`; only an independent verification receipt may do so. A queued retest creates a fresh server-owned scan from the original target/mode. A clean deterministic retest becomes `VALIDATED`, while engine-only absence remains `INCONCLUSIVE`; neither becomes independently verified. Manual `FIXED` requests remain `FIXED_PENDING_RETEST`, legacy unvalidated fixes stay scoreable, coverage receipts retain every limitation/subject in the manifest, and retries resume final scoring from the manifest rather than replaying a scan. Public sharing calls this progress “retest-confirmed,” never independently verified. Intrusive exploit replay remains authorization-gated and is not implemented. See `codebase.md` §40.

- **Deep Review v3 — identity and tenant controls (PR #54)**: agent approvals are consumed exactly once with atomic execution state; account deletion anonymizes referral/scorecard attribution; production requires an explicit trusted proxy IP header; RLS documentation and growth-table ownership tests match the actual service boundary.
- **Deep Review v3 — scan pipeline and MCP (PR #55)**: non-repository targets explicitly skip repository scanners; OSV calls are deduplicated/batched and source walks are bounded/symlink-safe; scanner phases and shutdown are time-bounded; infrastructure exits are categorized; evidence retries are idempotent. MCP mutating tools require interactive approval on the controlling terminal and fail closed when no terminal is available; stdout remains JSON-RPC-only.
- **Deep Review v3 — immutable truth and cost controls (PR #56)**: reports render immutable creation-time snapshots; launch readiness uses unpaginated database aggregates; scorecard publish responses return persisted counters; policy budgets are clamped by `PLATFORM_MAX_SCAN_BUDGET_USD` (default $50), and reported overages are retained and warned after execution.
- **Deep Review v3 — sharing and workflow edges (PR #57)**: scorecard visitor deduplication uses an HMAC-signed HttpOnly cookie; supersession is workspace-scoped; channel/referral allowlists are shared; notification bulk reads are tenant/user scoped; clipboard feedback and destructive dialogs are truthful/accessibly focused; score lookup has a composite index; the gitleaks workflow scans the exact base-to-head range.
- **Deletion/report compatibility (PR #59)**: account deletion preserves terminal referral attribution status, and legacy report downloads retain their safe live-data fallback while immutable snapshots remain the default for new reports.
- **Premium UI and reports (PR #60)**: theme persistence avoids first-paint flashes, shared controls meet mobile touch targets, deterministic UTC date formatting prevents hydration drift, and report share pages are `noindex` with `Referrer-Policy: no-referrer` and generic target/title disclosure.

- **Full-flow UI QA**: Astro waitlist submission now omits absent referral codes and preserves allowlisted `tools`/`tool` attribution; paginated APIs return the client-contract envelope; empty-workspace reports, notifications, and schedules render without crashing; team dates hydrate deterministically; launch readiness stays `NOT_EVALUATED` until a scan completes; and auth pages expose a main landmark.
- **Audit hash chaining**: `packages/db/src/client.ts` serializes each workspace chain with a transaction-scoped PostgreSQL advisory lock. Account deletion anonymizes user attribution and rehashes affected chains under the same lock.
- **Evidence storage**: `apps/worker/src/engine/evidence-storage.ts` uploads PoC and code-location artifacts to S3-compatible storage with `AES256` SSE and SHA-256 checksums. It fails closed when storage is missing or an upload fails; placeholder evidence is forbidden.
- **Prompt-injection guard**: `packages/mcp/src/prompt-injection-guard.ts` now normalizes input (zero-width chars, NFKC, HTML entities) and uses a tightened, expanded pattern set with explicit critical-pattern logic.
- **API client**: `apps/web/src/lib/api-client.ts` distinguishes caller cancellation from timeouts and propagates already-aborted signals.
- **Client-IP extraction**: the web app trusts only the header named by `TRUSTED_PROXY_IP_HEADER`; production ingress must strip client-supplied copies. Cloudflare marketing uses `cf-connecting-ip` and an atomic D1 fallback limiter.
- **GitHub token cache**: `packages/integrations/src/redis.ts` and `queue.ts` added; `packages/integrations/src/github.ts` caches installation tokens and `enqueueScan` is centralized.
- **Queue unification**: `apps/web/src/lib/queue.ts`, `apps/agent/src/queue.ts`, and `apps/worker/src/queue.ts` now consume `packages/integrations/src/queue.ts` (`getScanQueue`, `enqueueScan`).
- **Prettier scope**: `.prettierignore` excludes `.devin`, `.windsurf`, generated Astro/Wrangler files, and `next-env.d.ts`.
- **Privacy and E2E**: `DELETE /api/account` blocks sole owners, anonymizes loose attribution, preserves audit-chain validity, and is exposed in Settings. Playwright covers signup/signin, onboarding, target and scan creation, and cross-tenant scan/finding/report denial.
- **Operations**: `/api/health`, `/api/ready`, and Next.js request-error instrumentation are implemented. CI runs formatting and Chromium E2E. Docker contexts exclude generated output and the sibling engine virtualenv.

### UX V2 polish batch (2026-07-29)

- **Mobile safe-area and navigation:** `viewport-fit=cover` added to the app layout viewport metadata; the mobile page header respects `env(safe-area-inset-top)` and renders an accessible back button with `ChevronLeft` icon and `buttonVariants` styling; the dashboard main content top padding matches the increased header height.
- **Bottom sheet animations (superseded 2026-07-30):** the original enter/exit transitions on `BottomSheet` were followed by deleting the component entirely — see the 2026-07-30 batch below.
- **Shared label module (superseded 2026-07-30):** the label module is `apps/web/src/lib/labels.ts`, not `enum-labels.ts` (that path was never created). See below for what it covers now.
- **Ownership attestation:** URL target creation now requires an explicit `ownershipAttested: true` flag in `CreateUrlTargetSchema`. The API rejects creation without it and writes a distinct `target.ownership_attested` audit entry. The targets form includes a required checkbox.
- **Release verdict documentation:** the public methodology page (`/score/methodology`) now documents the release verdict scale: Go (≥80), Go with conditions (40–79), No go (<40), and Not evaluated.
- **Finding detail drawer:** retry and close actions are available on API error instead of a dead-end.
- **Retest tooltip:** the disabled "Queue fresh retest" button now explains what a linked scan is and how to get one.
- **Parallel queries:** `issues/page.tsx` and `products/page.tsx` now use `Promise.all` for parallel database queries instead of sequential awaits.
- **Font preload:** the Bricolage Grotesque hero typeface import is centralized in `global.css` alongside other font imports.
- **EvidenceWorld lazy island:** verified the IntersectionObserver-based lazy load with `rootMargin: "50% 0px"` and dynamic import.
- **Trust Command Center directive:** confirmed as a server component (no `"use client"` directive).
- **Bound queries:** `runs/page.tsx` bounds `target.findMany` with `take: 200`.
- **Evidence/approvals pages:** use lean `select` projections for database queries.

### UX V2 audit remediation, spend controls, and RLS proof (2026-07-30)

Full detail in `codebase.md` §59. Fixed three live P0s (mobile nav orphaning Approvals/Evidence/Automations; every pre-verdict public scorecard 500ing; the marketing deploy stranded by a path filter), closed 17 of 25 empty-state dead ends by making `EmptyState.action` a required `ReactNode | null`, stopped six screens from rendering raw database enums, deleted the hand-rolled `bottom-sheet.tsx` in favor of the Radix `Sheet` already used elsewhere, added a manual-refresh affordance to the in-progress scan view, grouped the desktop sidebar, added a per-workspace scan-creation rate limit (5/min) and a 3-concurrent-scan cap to bound spend, made the RLS fail-closed test actually run against a real `NOBYPASSRLS` role instead of a superuser that made it pass vacuously, made `LYRASHIELD_REQUIRE_EMAIL_VERIFICATION` load-bearing (it was previously read by no code), fixed the deploy workflow to actually run migrations and probe `/api/ready`, scheduled nightly backups, published `/vibe-security-50` generated from the live control registry, and closed several SEO/AEO gaps. Verified end-to-end against a real cloned repo with a local Postgres running the full 28-migration chain: 118 test files, 1055 tests, 0 failures, 0 skips; `pnpm build` 6/6; lint and typecheck exit 0 across all packages; format clean. Merged via PR #174 on branch `fix/uxv2-sweep-p0`.

### CLI, agent registry, and docs hardening (2026-07-31)

Closed the remaining `docs/audits/2026-07-31-agent-integration-deep-review-v10.md` P0/P1/P2 findings for the agent installer and MCP: bare-path CLI calls through `@lyrashield/sdk`, fail-fast `INVALID_PATH` guard in `buildUrl()`, data-driven `forceInlineEnv` and `serverNamePattern` in `packages/agent-registry`, removal of the dead `serverNameConstraint` field, JSONC-safe `renderConfig()` and `renderEntry()` paths, TOML/YAML merge-safety tests, and the `source { checkedOn, url }` field on every agent entry. The manual-install verification gate was run against real Windsurf, VS Code, and OpenAI Codex config files and the actual `@lyrashield/mcp` stdio binary. Docs package READMEs, `docs/audits/2026-07-31-agent-integration-coding-agent-handoff.md`, `codebase.md §60`, and this file were updated to reflect the current shape.

### Technical-debt cleanup and GitHub webhook precision (2026-07-31)

Removed launch-debt items from `apps/marketing`, `apps/web`, and `packages/*`: refactored the inline Lite Check JavaScript in `apps/marketing/src/pages/scan.astro` into named functions, extracted hardcoded polling/timeouts into named constants in `apps/web`, removed dead commented server-only exports from `packages/auth/src/index.ts`, wrapped Brevo `fetch` calls in `packages/auth/src/auth.ts` with `try/catch`, centralized SDK timeout/retry and agent-registry `checkedOn` constants, and added justification comments for security-related `eslint-disable` directives in `packages/security`, `packages/mcp`, `apps/worker`, and `packages/cli`.

Added `Target.installationId` to the Prisma schema with a supporting migration, regenerated the Prisma client, and updated `CreateRepoTargetSchema` to accept an optional `installationId`. `POST /api/targets` now stores the GitHub App installation ID on every repo target (from the request, or by resolving the workspace's active GitHub integration) and `POST /api/webhooks/github` deletes targets by exact `installationId` on `installation.deleted`, replacing the previous `repoFullName` `startsWith(owner/)` heuristic that could over-match. The repo-picker API and all three target-creation call sites (targets page, GitHub integration card, onboarding wizard) now thread `installationId` from the integration through to the target.

Verified: `git diff --check`, `pnpm lint`, `pnpm typecheck`, `pnpm test` (132/132), and `python3 .devin/scripts/checklist.py .` (6/6) all pass. The local Postgres database has been migrated to the 29th migration (`20260731104104_add_target_installation_id`).

### Installation-id authorisation and CLI/web UX hardening (PR #185, merged `9d0aa8a`, 2026-07-31)

An independent review of the installation-id work above (`docs/audits/2026-07-31-agent-integration-deep-review-v10.md`) found two real gaps and fixed both. `POST /api/targets` had accepted a client-supplied `installationId` verbatim (schema was a bare `z.string().optional()`); a caller could tag a target with a foreign or invented installation id, which then permanently escaped the App-uninstall cleanup above. The route now verifies the id belongs to an active GitHub integration in the same workspace (400 `INSTALLATION_NOT_FOUND` otherwise), and `CreateRepoTargetSchema.installationId` is constrained to a numeric string, max 20. Separately, the `installation.deleted` webhook's exact-match predicate could never match the `NULL` `installationId` on targets created before that column existed, so those legacy targets survived an App uninstall and stayed scannable after the customer revoked access; the cleanup now also covers that cohort via an exact `repoOwner` match (not the `startsWith` prefix bug the exact match originally replaced).

CLI: `scan --watch`/`status --watch` previously warned "not yet implemented" and exited `0`, so a CI script waiting on a scan believed it had completed; both now exit `2` before doing any work, and `scan` refuses before submitting so a retry cannot double-spend the workspace scan budget. `doctor` gives concrete remediation (checks to run, `lyrashield login`, available workspaces by id and name) instead of printing a raw connection error, and stops printing gotchas for agents that are not installed. `init` prints human-readable outcomes ("needs manual setup") instead of the raw `MANUAL_REQUIRED`-style enum; `--json` output keeps the machine codes.

Web/marketing: the reports client no longer discards the server's error message in bare `catch {}` blocks; `share-sheet.tsx`'s copy-feedback timer is cleared on unmount; the onboarding workspace-name step submits on Enter; the marketing lite scanner's fetch now has a 30s `AbortController` (previously could hang indefinitely on an unreachable target) and the Turnstile boot loop is bounded (previously polled every 100ms forever if a script was blocked); every docs code block got a copy button.

Verified: `pnpm lint` clean (marketing/web/cli), `astro check` clean for changed files, full suite passed with no regressions against the pre-change baseline. CI green on the merge commit. Process note: the first push to this PR failed CI on `format:check` (prettier) because the local verification pass had run lint and tests but skipped it — a reminder to run the full documented gate, not a subset.

## Current execution queue

### 1. Controlled scan release gate

Owner: engineering + founder authorization.

1. Keep `LYRASHIELD_LUNA_LLM`/`LYRASHIELD_TERRA_LLM` and the tested `LYRASHIELD_LLM` fallback authorized for the target environment. A production Standard/Luna scan has been completed (2026-07-29, see verified state above); Deep/Terra still needs its own approved run. Engine PRs #6, #7, and #20 are merged and enforce the bounded context, output, agent, pre-request spend, and cache-read usage-accounting contract.
2. Promote only an inspected sandbox image digest; the local compose default is digest-pinned, but production image provenance and approval remain operational gates.
3. Provision the full-scan runtime: a BullMQ-compatible TLS Redis connection (not only Upstash REST credentials), private S3-compatible evidence storage, dedicated worker compute with Git/engine CLI/pinned sandbox access, and the authenticated Next.js application origin. The live Lite Scanner container is a separate passive API and is not this runtime.
4. **Done for Standard/Luna (2026-07-29):** an approved production target (`ecryptoguru/OnboardingAI2`) completed the full worker lifecycle with retained scan events, findings, coverage receipts, and a tamper-evident manifest. Deep/Terra remains pending.
5. Keep Docker health, engine CLI availability, local sandbox execution, and production controlled-scan proof as separate claims.

### 2. Billing and usage limits

Owner: engineering + founder pricing decision.

Sprint 10 is the principal unbuilt self-serve Phase 1 feature. Confirm the provider, plan boundaries, usage metric, quotas, and retry/concurrency policy before implementation. Existing billing models and environment variables are schema foundation only; do not publish draft pricing.

### 3. Production observability and recovery

Owner: engineering.

Connect the implemented structured logs and web/scan readiness signals to the selected monitoring platform. Define alerts and incident ownership, then prove backup/restore, worker cancellation, queue recovery under production failure injection, and capacity in the target environment. PR #115 proves fail-closed queue admission/reconciliation and local Docker recovery, not production alert delivery or disaster recovery.

### 4. Deployment defense in depth

Owner: engineering / infrastructure.

Provision production data stores, evidence storage, secrets, TLS, backups, monitoring, and worker capacity. Add transport-level egress control with DNS pinning before exposing scans to untrusted targets at scale; application SSRF checks are not a substitute.

### 5. Marketing launch gate

Owner: founder + marketing + engineering.

1. **Done:** use `https://lyrashieldai.com` as the canonical marketing origin; trademark clearance remains separate.
2. **Done:** production D1, Rate Limit, KV, and `WAITLIST_IP_SALT` bindings are provisioned; D1 migrations `0001`–`0003` are applied remotely.
3. **Done:** the indexable marketing Worker is deployed from Astro's generated `dist/server/wrangler.json`; apex TLS, waitlist/referral APIs, canonical metadata, sitemap/robots/`llms.txt`, schema, security headers, Cloudflare analytics compatibility, internal links, and representative mobile Lighthouse/desktop Brave QA pass on the real domain.
4. **Done:** `www` permanently redirects to the apex with path and query preservation.
5. **Done:** GitHub Actions auto-deploys marketing-impacting `main` changes only after the security and full release gates, using an account-owned least-privilege Cloudflare token; the job applies D1 migrations, deploys the generated Worker config, and smoke-checks production. Rotate the token before July 16, 2027.
6. **Done for the passive Lite Scanner:** `PUBLIC_SCANNER_URL` points to the separately protected Azure Container App, Turnstile and the monitored abuse route are live, `/scan` is enabled and included in the sitemap, and production browser/API checks pass.
7. **Done for open registration:** `PUBLIC_APP_URL` is set to `https://app.lyrashieldai.com`, the authenticated app accepts email and configured OAuth sign-up, and marketing CTAs link to sign-up/sign-in. Separately verify scorecard metadata, all card formats, badge output, revocation/expiry 404s, referral attribution, and human-event deduplication. Submit the sitemap in the selected search-engine webmaster accounts when ownership access is available. Publish only founder-approved posts; sample posts remain drafts by design.

## Deferred roadmap

- Security Copilot sidebar and visual security plan/recap
- Compliance-lite evidence packs
- IaC, container, and reachability scanning
- Enterprise identity, SCIM, advanced policy, private worker, VPC/self-hosting, and BYOK/BYOM

## Founder decisions still needed

- Trademark clearance for the confirmed `lyrashieldai.com` public domain
- Pricing, plan boundaries, usage metric, and billing provider
- Public-launch timing
- Authorized model/provider and first controlled-scan target
- **`eval-exec` gate severity (self-flagged 2026-07-31, never confirmed):** `packages/cli/src/diff-core.ts` classifies `eval`/`exec` usage in a diff as `MEDIUM` (was `HIGH`); `action.yml`'s equivalent risky-pattern check was already `MEDIUM`. Both `lyrashield gate` and the GitHub Action default to `--fail-on HIGH`, so as of this change **an `eval()`/`exec()` addition in a diff will not fail either gate by default.** `codebase.md`'s working notes on this change say "a security review should confirm this is the intended product behavior before merge" — it already merged (main, `packages/cli/src/diff-core.ts:38-40`) without that confirmation. Needs an explicit founder call: keep both at `MEDIUM` (current state, internally consistent but permissive), or raise both to `HIGH` (stricter default, matches how a security product's own reviewer would likely want to be treated).

## Non-negotiable implementation rules

- Never push directly to `main`; use a focused branch and PR.
- Scope every workspace query by `workspaceId`, validate input with Zod, use `@lyrashield/logger`, and write audit events for sensitive operations.
- Use shared UI components and typed API helpers; inspect rendered UI for frontend changes.
- Verify relevant work with `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`, and `git diff --check`. Add security regression coverage for security controls.
- Keep public claims honest: no benchmark/accuracy claims, pricing, customer claims, or public mention of the forked engine.

## Landmines

- `SOFT_DELETE_MODELS` and `WORKSPACE_SCOPED_MODELS` must include only Prisma models that actually have the corresponding column. `WorkspaceMember` and `OnboardingState` are deliberately excluded from automatic workspace scoping.
- Workspace context uses `AsyncLocalStorage`; never replace it with module-level state. For database RLS, use `withWorkspaceRLS(workspaceId, fn)` so `SET LOCAL` stays connection-safe.
- Keep the `Schedule.targetId` foreign key migration. Worker-created notifications are workspace-level and must not disappear behind a default `userId` filter. Use `createAndSendNotification` rather than duplicating its create/send loop.
- The engine is a controlled derivative: do not perform mechanical Strix-to-LyraShield rewrites or move generic upstream plumbing without a concrete requirement. Its upstream workflow may arm squash auto-merge only after requesting owner review; branch protection must still require that approval and green Engine CI. Never bypass those gates, force-push, or resolve conflicts automatically.
- Cloudflare marketing deployment must use Astro's generated `dist/server/wrangler.json`; root `wrangler.jsonc` is for bindings and build configuration.
- **Audit logging**: create audit logs through the extended Prisma client (`prisma.auditLog.create()`). Do not write audit events inside a broader Prisma transaction; the extension owns the advisory-locked chain transaction. Any mutation of hashed fields requires a locked chain rebuild.
- **Evidence**: every `Evidence` record must go through `uploadEvidence()` with a valid `encryptionKeyRef` and checksum. Missing/unavailable storage is a hard failure; never restore `encrypted://` placeholders.
- **Proxy trust**: set `TRUSTED_PROXY_IP_HEADER` only when ingress strips incoming copies and writes the authoritative client IP.
- **Prompt-injection guard**: use `PromptInjectionGuard` and `normalizeInput()` for any new model-facing input checks; do not reintroduce ad-hoc regex bypasses.
- **Queue and scan job**: use `enqueueScan` and `getScanQueue` from `packages/integrations/src/queue.ts`; do not create one-off `Queue` instances or duplicate scan-enqueue logic in web/agent/worker.
- **Engine boundary**: invoke the external engine only for `REPO` targets. URL targets use the pinned deterministic URL scanner until the engine has an equivalently pinned transport contract.
- **Engine output**: keep artifact byte limits and parser field/count limits. Treat engine output as untrusted; only trusted verifier evidence may set a finding to verified.
- **Result integrity**: confidence is triage metadata, never proof. Persist new scan claims through the result manifest, coverage receipt, candidate, and verification-receipt boundaries. A clean retest is `VALIDATED` only when the originating deterministic scanner had complete coverage; engine-only retests are `INCONCLUSIVE`. Direct status updates must not set terminal `FIXED`; retain `FIXED_PENDING_RETEST` until the server-owned retest records its receipt. Keep every coverage limitation in the immutable manifest, and resume only final scoring when a retry finds that checkpoint.
- **GitHub installations and Fix PRs**: never create a workspace integration from callback state alone, and never accept a client-authored patch, branch, title, or body for PR creation. Restore either flow only with a provider ownership assertion or immutable server-generated patch bound to the consumed approval.
- **Model accounting**: keep model selection in `resolveEngineProfile()`, protected limits in `resolveScanBudgetUsd()`, and official GPT-5.6 rates in the versioned `apps/worker/src/engine/gpt56-pricing.ts` rate card. Preserve per-request standard/long-context buckets, cache-read/cache-write separation, private accounting events, and the cost-free UI. Do not bypass positive policy validation, remove the fallback model, silently guess ambiguous aggregate pricing, or claim that mode routing is a within-scan Luna→Terra cascade.
- **Public sharing analytics**: use the strict `/api/scorecards/events` allowlist and `recordScorecardEvent()`. Do not put target/repository/finding data, raw IPs, user agents, or user-authored captions into `ScorecardEvent`; social image/page renders are not human views.
- **Production database migrations (FIXED 2026-07-30, was a landmine before this):** the Azure deploy workflow (`deploy-azure.yml`) now runs `pnpm --filter @lyrashield/db exec prisma migrate deploy` against `secrets.DATABASE_DIRECT_URL` immediately before the container image update, and fails the deploy loudly if that secret is unset. Before this fix the workflow built and pushed images and ran health smoke checks but never migrated production — which is exactly how the 2026-07-29 scan failure happened (`Finding.statusReason` existed locally, was never applied to production). New migrations must still be additive/backward-compatible with the currently running image, since the deploy applies schema before it swaps code. Both smoke checks were also moved from `/api/health` (a static `"ok"`) to `/api/ready` (checks database and Redis), since a static-ok endpoint would pass with the database down.
- **RLS runtime role**: `DATABASE_URL` must connect as a role with `rolsuper = false` and `rolbypassrls = false`. `FORCE ROW LEVEL SECURITY` (applied to all 21 workspace-scoped tables since `20260721033000_strict_workspace_rls`, and to the 9 child tables since `20260803000001_child_table_rls` — though those nine were only genuinely protected once `20260803000002_child_table_rls_enable` added the missing `ENABLE ROW LEVEL SECURITY`; **`FORCE` without `ENABLE` is a no-op and leaves every policy on the table inert**, which is exactly what shipped between those two migrations) binds the table _owner_ to policies but does nothing to a superuser or any `BYPASSRLS` role — a superuser connection makes every RLS policy inert regardless of `FORCE`, which is easy to inherit by accident from a managed Postgres provider's default user. Verify with `SELECT rolsuper, rolbypassrls FROM pg_roles WHERE rolname = current_user;` — both must be `false`. See `docs/deployment/PRODUCTION_DEPLOYMENT.md` "Known production blockers" §2 for the role-creation SQL. `packages/db/src/rls-fail-closed.test.ts` enforces this in CI by refusing to run its assertions (rather than passing vacuously) against a role that can bypass RLS. **Current state (2026-08-07):** all 30 tables are RLS-protected — the 21 workspace-scoped tables plus the 9 child tables (`ScanEvent`, `Evidence`, `ScanResultManifest`, `ScanCoverageReceipt`, `FixProposal`, `PullRequest`, `Ticket`, `ScorecardShare`, `ScorecardEvent`), which were re-enabled by `20260807000003_child_table_rls_re_enable` after the `42501` outage was root-caused to a single write path (`account-deletion.ts` updating `ScorecardShare` outside `withWorkspaceRLS`). Application-layer enforcement (`withWorkspaceRLS` at 43 call sites + service-layer parent-ownership verification in `addScanEvent` et al.) remains in place as defence in depth. The child-table fail-closed tests are un-skipped, and the `rls-child-write-repro` CI job runs them against real Postgres under a restricted role on every push. **Lesson worth keeping:** the original gap survived CI because the tests only asserted cross-workspace reads return zero rows — a policy that always evaluates false also returns zero rows. Any new RLS policy needs a same-workspace **write** assertion to be meaningfully covered.
- **Email verification and shared rate limiting are enforced at different times, deliberately.** `LYRASHIELD_REQUIRE_EMAIL_VERIFICATION` (default `"1"`, currently pinned to `"0"` in production — see the blockers doc) is validated at **boot** in `packages/config/src/env.ts`, because it is about not claiming to verify addresses the app cannot mail. `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` are validated at **deploy** in `deploy-azure.yml`, not at boot — boot validation fires in every production-mode process including the Playwright E2E server, and would fail a _running_ app on restart, trading a rate-limiting weakness for an availability outage. Do not move either check to the other's enforcement point without re-deriving why it lives where it does.

## Repository boundaries and generated artifacts

- `lyrashield-engine` (sibling repo) owns the Python scanner/runtime implementation; product code in this repo only invokes it through the worker.
- `lyrashield-marketplace` (sibling repo) owns published public install artifacts; generation source lives here in `packages/agent-plugin` and `docs/marketplace`, but the committed releases belong in the marketplace repo.
- `lyrashieldai` (this repo) is the authoritative source for product, auth, API, dashboard, MCP, installer source, and generation/conformance checks.
- `@lyrashield/cli` is deprecated; the canonical published CLI is the unscoped `lyrashield` package.

Build artifacts and generated media are **not kept in this repo**:
- `apps/web/.next/`, `apps/marketing/dist/`, `packages/*/dist/`, `.turbo/`, `apps/marketing-motion/renders/`, and `apps/marketing/public/media-local/` are generated and `.gitignore`d.
- Heavy media masters/renders are archived to remote storage (R2) or regenerated by the `apps/marketing-motion` workflow.
- Run `pnpm install` and `pnpm build` as needed; do not commit `node_modules` or build output.

## Documentation ownership

- `PRD.md` — strategy, authoritative backlog, accepted/rejected work
- `codebase.md` — architecture, code map, implementation history
- `AGENTS.md` — current state, execution queue, rules, and landmines
- `product.md` — current positioning and founder decisions
- `userguide.md` — complete end-user workflows, options, result language, roles, and limitations
- `apps/marketing/README.md` / `BLOG_AUTHORING.md` — marketing operations and publishing rules
- `docs/deployment/*` — local and production runbooks

After a PR merges, remove branch-only wording and record the PR/merge truth here, in `PRD.md` Part C, and in the relevant dated `codebase.md` section. Do not update historical checkpoint counts except when explicitly labeling them as current.
