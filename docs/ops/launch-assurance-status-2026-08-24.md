# Launch assurance status — 2026-08-24

This record separates code and local evidence from production and external evidence. It is a release-gate snapshot, not a launch approval, deployment record, certification, or claim of universal security.

> Historical snapshot: superseded for current deployment and Standard-scan truth by [AGENTS.md](../../AGENTS.md), [PRD.md](../../PRD.md), and [codebase.md](../../codebase.md). On 2026-08-26, release run `32916256313` deployed product `7abd9baa`, worker digest `sha256:fd1888ccaedc9d9f2618398c1924571f23a03fed05efb6b772c922cf43d7cf01`, and engine `852b1ed7`. Standard scan `cmt9el7p7000001hdjnjo90wk` completed with sealed source/coverage/accounting receipts. Hard stops not explicitly closed by current truth documents remain open.

## Release dependency

Product PRs #411–#424 merged through `80460f80d32f42e1a647eed180be6a3fa9f4bf51`. PRs #421–#424 added provider-environment deployment gates, hardened billing verification, short-lived Desktop sync sessions and output redaction, stronger platform-admin identity boundaries, Agent Plugin `0.1.18`, and build/runtime environment isolation. Required CI for `80460f80` is green. Engine PR #97 remains the pinned runtime `944a84f15f913909039c89146c25db650cd87137`; documentation PRs #98–#100 merged afterward without changing that pin. Marketplace PR #12 merged as `fb9758f`, CI run `32757331581` passed, and immutable `v0.1.18` is published. Its 26 generated artifact groups are byte-identical to product `80460f80`, and archive checksums were verified.

Current assurance hardening PRs #428–#430 merged through `3966e24c`. PR #428 added nonnegative policy budgets (PostgreSQL check constraint `Policy_maxBudgetUsd_nonnegative`), explainable deterministic finding priority with limitation disclosure, immutable retest validation bound to stored manifests, and removal of raw evidence-storage URIs from finding detail. PR #429 added worker execution provenance (product revision, worker image digest, engine revision) bound into manifest v5 checksums with production fail-closed readiness, actionable Azure alert provisioning with readback and idempotent reruns, and a bounded host-side dry-run-first launch-assurance orchestrator composing existing evidence, cancellation, and queue-reconciliation paths. PR #430 fixed test mock typing. Required CI for `3966e24c` is green; that code is not deployed.

Production release run `32738811470` completed successfully for `8347fda9`. It applied forward migrations, deployed and promoted app `lyrashield-app--0000170`, scanner `lyrashield-scanner--0000151`, and egress proxy `lyrashield-egress-proxy--0000020`, then passed candidate and public smoke. The separately promoted worker digest `sha256:6f73ad5e1125fffd8b4eec85103d14b49eb0c6a1765cab29a1a5edb3d7a17413` has product label `8347fda9`, engine label `944a84f`, healthy Docker state, and successful external scan-readiness readback; the prior worker configuration is retained for rollback.

Release run `32755678337` for `80460f80` built, verified, and pushed web, worker, and egress-proxy images, then failed closed at the protected provider-configuration check because the stored Razorpay credential is Test Mode and production requires Live Mode. Azure login, migrations, secret synchronization, revision creation, promotion, worker rollout, and runtime mutation did not run. The pushed images are build artifacts only, not deployment evidence. Production remains healthy on `8347fda9`, and all Cloud and Local purchase admissions remain `off`.

## Code-ready and local evidence

| Gate                    | Current evidence                                                                                                                                                                                                                                             | Status                    |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------- |
| Engine revision         | Runtime pin `944a84f15f913909039c89146c25db650cd87137`; the worker OCI label matches it. Engine PR and `main` CI passed verify, Pyright with zero errors, 1,302 tests with one skip, sandbox smoke, and Desktop packaging.                                   | Verified and deployed     |
| Product worker contract | Product CI and release both checked the exact engine provenance and worker contract. The deployed worker OCI label is `8347fda923032960661079491a0a17956aebefd9`; its non-root user, vendored entrypoint, exact digest, and healthy runtime were read back.  | Verified and deployed     |
| Marketplace artifacts   | Marketplace commit `fb9758f` passed CI run `32757331581`; all 26 generated artifact groups are byte-identical to product `80460f80`, archive checksums were verified, and immutable tag `v0.1.18` is published. Channel review and listing versions may lag. | Published export verified |

The engine pin establishes source provenance and compatibility only. It does not prove that a corresponding production worker or sandbox image was built, published, promoted, or is running.

## Production and external hard stops

Every gate below remains **INCONCLUSIVE** until retained evidence is bound to the exact product commit, engine commit, immutable image digest, configuration or policy revision, and timestamp where applicable.

| Hard-stop gate                        | Required acceptance evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Current status                              |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------- |
| Exact sandbox image                   | Publish the exact reviewed sandbox candidate, smoke the same digest on supported architectures, verify promoted tags resolve to that digest, retain SBOM and provenance attestations, and bind the digest to runtime configuration. Engine CI passed for runtime pin `944a84f`, but this release did not re-verify and retain the separately configured production sandbox digest and attestations.                                                                                                                                                                      | INCONCLUSIVE                                |
| Evidence storage live probe           | In production, prove private S3-compatible upload through `uploadEvidence()`, client-side encryption, checksum and key reference retention, authorized retrieval, workspace isolation, and fail-closed behavior for missing or invalid storage/encryption configuration. The `verify:launch-assurance` orchestrator (PR #429) composes the fail-closed and round-trip proofs in code; production execution with exact scan/workspace IDs and the confirmation phrase remains the gate.                                                                                   | INCONCLUSIVE — hard stop (code ready)       |
| Actionable alert readback             | Trigger and read back actionable readiness, queue, provider, model-cost, worker-liveness, and failure alerts; retain routing, incident owner, threshold, timestamp, and acknowledgement evidence. A configured rule without delivered/read-back notification is insufficient. `provision-alerts.sh` and the monitoring runbook (PR #429) define the rule inventory, action-group binding, readback, and idempotent provisioning; production provisioning and a test-alert acknowledgment remain the gate.                                                                | INCONCLUSIVE — hard stop (code ready)       |
| Production cancellation and recovery  | Inject a controlled production worker/queue failure, cancel active work safely, prove terminal state and resource cleanup, recover queue authority without deleting BullMQ keys or replaying ambiguous provider-billable work, and reconcile readiness afterward. The `verify:launch-assurance` orchestrator (PR #429) composes authenticated cancellation, settle wait, `reconcileScanQueue`, and post-recovery readiness in code; production failure injection with exact scan/workspace IDs and the confirmation phrase remains the gate.                             | INCONCLUSIVE — hard stop (code ready)       |
| Non-exportable production signing     | Prove Azure Key Vault managed-identity signing and verification with a non-exportable production key, least-privilege access, audit evidence, revocation handling, and fail-closed behavior. Local or file-backed keys do not satisfy this gate.                                                                                                                                                                                                                                                                                                                         | INCONCLUSIVE — hard stop                    |
| Provider entitlement and usage events | For each enabled provider, retain signed test-mode webhook/event evidence, idempotent application processing, entitlement state, and usage-meter reconciliation. Live-provider events require explicit founder authorization; test objects must not be presented as live-payment proof.                                                                                                                                                                                                                                                                                  | INCONCLUSIVE — hard stop                    |
| External scorecard distribution       | Verify public scorecard metadata, supported card formats, badge, revocation and expiry, referral and human-event deduplication, real external unfurls, and webmaster submission/readback. Local rendering alone is insufficient.                                                                                                                                                                                                                                                                                                                                         | INCONCLUSIVE — hard stop                    |
| Standard finding disposition          | Export and retain all 24 findings from the accepted Standard scan with exact target/revision provenance, triage each finding, obtain independent verification where warranted, apply approved fixes, and run trusted deterministic retests. Engine-only absence or confidence cannot produce `VALIDATED`; incomplete evidence remains `INCONCLUSIVE`.                                                                                                                                                                                                                    | INCONCLUSIVE — hard stop                    |
| Deep/Terra acceptance                 | Obtain founder approval for provider/model, target, paid budget, and timing. Then run one controlled Deep acceptance bound to exact app/engine/image revisions, prove Terra-root/Luna-specialist routing, terminal state, complete per-request accounting, reconciled cost, evidence/coverage receipts, findings, and cleanup.                                                                                                                                                                                                                                           | Awaiting founder approval — hard stop       |
| Platform-admin account proof          | Run exact-two preflight after both named users are unique, email-verified, and personally TOTP-enrolled; only then apply roles, revoke prior sessions/elevations, retain bootstrap audit, and capture fresh-session MFA browser proof. Read-only main run `32733611844` proved the isolated client reached account validation and failed because verified account `ecryptoguru@gmail.com` has not enrolled TOTP. The script stopped at that first fail-closed result, so `ankit@lyrashieldai.com` was not evaluated; no apply or account/session/role mutation occurred. | Personal TOTP required; no account modified |

## Provider console evidence

A read-only Brave review on 2026-08-24 established configuration state only:

- Razorpay Test Mode showed six Cloud plans. Live Account Activation was complete and the website was approved, but the live plan catalog was empty and no live webhook was configured. Hosted-checkout payment-method behavior above INR 15,000 was not transaction-proven.
- Polar Sandbox showed its sandbox boundary, no active products, and no active organization token. Polar Live setup was 5/7; identity verification remained pending and gates payouts, active products were empty, and only archived private test products existed.

No charge, credential creation, new financial terms, admission change, or provider mutation was performed. This is not live checkout, webhook, entitlement, settlement, or payout proof.

## Actions intentionally not performed

The current production release deployed product `8347fda9` and its worker only. The newer `80460f80` images were not deployed. No work activated Polar or Razorpay live billing, created live provider credentials/catalogs/webhooks, initiated RazorpayX or Payoneer payouts, ran production failure injection, submitted webmaster changes, provisioned either platform-admin account, or ran a paid Deep acceptance. Those gates remain separate and founder-controlled where stated.

## Launch decision

The deployed open beta is healthy, but code and runtime evidence are not interchangeable with external proof. General paid launch remains blocked while any hard-stop gate above is `INCONCLUSIVE` or while founder-controlled live billing and Deep acceptance decisions remain outstanding.
