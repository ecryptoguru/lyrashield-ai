# Coding-agent integration contract audit — 2026-09-08

This audit compares every entry in `packages/agent-registry` with the client owner's current documentation. Documentation proves the configuration contract only. It does not prove an authenticated client session, approval-bound mutation, revocation, restart, or upgrade.

All 30 source URLs returned HTTP 200 on 2026-09-08. The registry keeps 24 client entries and six package variants, resolving to 26 preferred client surfaces.

## Client contracts

| Client                           | Official source                                                                                                                                                          | LyraShield path                                                                      | Audit result                                                                                                                  |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- |
| Claude Code                      | [MCP](https://code.claude.com/docs/en/mcp), [plugins](https://code.claude.com/docs/en/plugins)                                                                           | Project `.mcp.json`, documented `claude mcp add` form, or Claude marketplace         | Corrected the obsolete CLI flags; marketplace activation is explicit.                                                         |
| Cursor                           | [MCP](https://cursor.com/docs/mcp), [plugins](https://cursor.com/docs/plugins)                                                                                           | `.cursor/mcp.json` or local Agent Plugin                                             | Existing root key, HTTP type, and plugin path retained.                                                                       |
| Devin                            | [MCP](https://docs.devin.ai/work-with-devin/mcp)                                                                                                                         | Settings, MCP Marketplace, Add Your Own                                              | UI guidance retained; no local file is invented.                                                                              |
| VS Code                          | [MCP servers](https://code.visualstudio.com/docs/agent-customization/mcp-servers), [Agent Plugins](https://code.visualstudio.com/docs/agent-customization/agent-plugins) | `.vscode/mcp.json` with `servers`                                                    | Config-file path remains preferred; unregistered plugin staging is experimental.                                              |
| OpenAI Codex                     | [MCP](https://developers.openai.com/codex/mcp), [plugins](https://developers.openai.com/plugins/build/plugins)                                                           | `~/.codex/config.toml` or Codex marketplace install                                  | Fixed `env` versus `env_vars`; plugin install now registers the marketplace before installing the generated Codex package.    |
| Cline                            | [Configuration](https://docs.cline.bot/getting-started/config) and [source](https://github.com/cline/cline/blob/main/sdk/packages/shared/src/storage/paths.ts)           | `~/.cline/data/settings/cline_mcp_settings.json` for CLI; extension UI for IDE hosts | Replaced the stale CLI path with the resolver-backed file, preserved `CLINE_MCP_SETTINGS_PATH`, and kept `streamableHttp`.    |
| OpenCode                         | [MCP servers](https://opencode.ai/docs/mcp-servers/)                                                                                                                     | Project `opencode.json` or `opencode.jsonc`, root `mcp`                              | Current `mcp`, `enabled`, local command-array, remote URL, and environment-reference contract retained.                       |
| Kilo Code                        | [MCP](https://kilo.ai/docs/automate/mcp/using-in-kilo-code)                                                                                                              | `kilo.jsonc`, root `mcp`                                                             | JSONC-safe merge, command array, `environment`, and `{env:VAR}` retained.                                                     |
| Zed                              | [MCP](https://zed.dev/docs/ai/mcp)                                                                                                                                       | `~/.config/zed/settings.json`, root `context_servers`                                | Nested `command.path`, arguments, and environment retained.                                                                   |
| Gemini CLI                       | [MCP](https://geminicli.com/docs/tools/mcp-server/)                                                                                                                      | `~/.gemini/settings.json`, root `mcpServers`                                         | Current supported client retained; `$VAR_NAME` references, `httpUrl`, trust, and tool allowlists match the current schema.    |
| JetBrains AI Assistant and Junie | [MCP](https://www.jetbrains.com/help/ai-assistant/mcp.html)                                                                                                              | Settings UI                                                                          | UI-only guidance retained.                                                                                                    |
| Amp                              | [MCP](https://ampcode.com/docs/customize/mcp)                                                                                                                            | `amp mcp add`                                                                        | Official source refreshed; stdio and inherited shell credential retained.                                                     |
| Pi                               | [Usage](https://pi.dev/docs/latest/usage), [extensions](https://pi.dev/docs/latest/extensions)                                                                           | Standalone LyraShield CLI or separately reviewed Pi extension                        | Removed the invented native MCP config. Pi explicitly keeps MCP out of core.                                                  |
| OpenClaw                         | [MCP CLI](https://docs.openclaw.ai/cli/mcp)                                                                                                                              | `openclaw mcp` or Control UI                                                         | Uses repeated `--arg` flags for local servers, `streamable-http` for remote servers, and `doctor --probe` for live proof.     |
| Hermes                           | [MCP config](https://hermes-agent.nousresearch.com/docs/reference/mcp-config-reference)                                                                                  | `~/.hermes/config.yaml`, root `mcp_servers`                                          | Uses `${env:LYRASHIELD_API_KEY}` so the key is resolved from Hermes' secret scope instead of stored inline.                   |
| Antigravity                      | [MCP](https://antigravity.google/docs/mcp), [migration](https://www.antigravity.google/docs/gcli-migration)                                                              | Global `~/.gemini/config/mcp_config.json` or project `.agents/mcp_config.json`       | `serverUrl` retained; global config is private while the project config remains shareable.                                    |
| GitHub Copilot CLI               | [MCP servers](https://docs.github.com/en/copilot/how-tos/copilot-cli/customize-copilot/add-mcp-servers)                                                                  | `~/.copilot/mcp-config.json`, `.mcp.json`, or `.github/mcp.json`                     | Local and HTTP types and all documented scopes retained.                                                                      |
| Goose                            | [Extensions](https://github.com/aaif-goose/goose/blob/main/documentation/docs/getting-started/using-extensions.md)                                                       | Goose Extensions UI/CLI or `~/.config/goose/config.yaml`                             | Replaced the stale docs host with the owner repository; stays guided because the nested YAML contract needs client ownership. |
| Aider                            | [Configuration](https://aider.chat/docs/config/aider_conf.html)                                                                                                          | Standalone `lyrashield check-diff` or `lyrashield gate --verdict`                    | Removed unsupported `mcp-servers` and `--mcp-servers` claims.                                                                 |
| Devin CLI                        | [MCP configuration](https://docs.devin.ai/cli/extensibility/mcp/configuration)                                                                                           | `.devin/config.local.json`                                                           | Official source refreshed; local stdio contract retained.                                                                     |
| Roo Code                         | [MCP](https://roocodeinc.github.io/Roo-Code/features/mcp/using-mcp-in-roo/)                                                                                              | `.roo/mcp.json`                                                                      | `mcpServers` and `streamable-http` retained.                                                                                  |
| MiMo Code                        | [MCP servers](https://mimo.xiaomi.com/mimocode/mcp-servers)                                                                                                              | `.mimicode/mimocode.jsonc` or user JSONC config                                      | Corrected the format to JSONC so installer merges preserve comments.                                                          |
| Codebuff                         | [MCP servers](https://www.codebuff.com/docs/tips/mcp-servers), [agents](https://www.codebuff.com/docs/agents/overview)                                                   | Project `.agents/mcp.json` or global `~/.agents/mcp.json`                            | Current docs confirm stdio and HTTP entries plus `$VAR_NAME` environment references; registry and installer now follow them.  |
| Oh-My-Pi                         | [MCP config](https://github.com/can1357/oh-my-pi/blob/main/docs/mcp-config.md)                                                                                           | `.omp/mcp.json` or profile-aware user config                                         | Official project source and HTTP/stdio shapes retained.                                                                       |

## Package variants

| Package surface             | Activation contract                                                                                                 | Result                                                                                                                               |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| Claude Code Agent Plugin    | Add `ecryptoguru/lyrashield-marketplace`, then install `lyrashield@lyrashield-ai`                                   | CLI and public guide now print the marketplace commands instead of copying into an undocumented directory.                           |
| Cursor Agent Plugin         | Copy to `~/.cursor/plugins/local/lyrashield`                                                                        | Package-conformance path retained.                                                                                                   |
| OpenAI Codex Agent Plugin   | `codex plugin marketplace add ecryptoguru/lyrashield-marketplace`, then `codex plugin add lyrashield@lyrashield-ai` | The CLI performs both registration steps; the marketplace points at a dedicated Codex root whose `.mcp.json` uses `streamable-http`. |
| VS Code Agent Plugin        | Install from Source, configured marketplace, or `chat.pluginLocations`                                              | Remains experimental; CLI directs users to the supported `.vscode/mcp.json` path.                                                    |
| GitHub Copilot Agent Plugin | Copilot marketplace or repository/local-path install                                                                | CLI and public guide now use Copilot's plugin manager; no private-directory auto-discovery claim remains.                            |
| Kiro compatibility package  | Merge `.mcp.kiro.json` into Kiro's workspace or user MCP file                                                       | CLI and public guide describe the required merge; a staged plugin folder is not treated as installed.                                |

## Runtime evidence boundary

The registry labels documentation or package conformance as `COMPATIBLE` or `EXPERIMENTAL`; `NATIVE` and `VERIFIED` require a retained client-runtime receipt with client version and platform.

The six-client desktop acceptance remains separate. Restarted desktop sessions for Devin 3.8.20, Antigravity 2.12.2, OpenCode Desktop 1.18.29, and Hermes 0.21.1 completed authenticated workspace reads, and an unapproved mutation was denied in each host. These receipts establish installability, authenticated reads, restart behavior, and fail-closed mutation behavior. Later receipts below cover approved writes and the lifecycle cases that were exercised.

After the user restarted Codex, Desktop 26.901.51231 exposed all 14 installed LyraShield tools: workspace discovery, target discovery, and Gate v2 readiness returned successfully in the desktop task. The legacy target correctly returned `INSUFFICIENT_EVIDENCE` with `ASSESSMENT_UNAVAILABLE` and `EXPECTED_IDENTITY_REQUIRED`. A developer-report request was denied because the OAuth connection has no write scope; no report was created.

Hermes displayed the exact-input approval prompt, but accepting it returned empty form content. Inspection of the installed `tools/mcp_tool.py` confirmed `ElicitResult(action="accept", content={})`, which omits the required `approve` field. LyraShield rejects this invalid response and its terminal fallback remains unavailable in the desktop host. This is a host form limitation, not a successful approved write.

Claude Desktop 1.46388.4 was checked again after restart: the tested Free account's Code tab still displays “Get Claude Code.” Current [Claude Desktop documentation](https://code.claude.com/docs/en/desktop#shared-configuration) confirms that local Code sessions read `~/.claude.json` and `.mcp.json`, and also load `claude_desktop_config.json` servers. The standalone CLI does not read the latter file. Configuration compatibility does not remove the observed account-entitlement block.

The approval compatibility review found that remote tool schemas did not advertise the `approvalId` required to resume a browser-approved call, and that the remote gate compared the input hash using the stored action name without rejecting a different requested tool. The expiry precheck also accepted the exact expiry instant, unlike the database execution claim. Regressions cover remote schema discovery, cross-tool reuse and exact expiry in pending/approved/executed states, malformed approval IDs, and empty elicitation content.

The fix merged in product commit `65a36918cf0831237a672477c71a08bd9d88a144`. Exact-main CI run `34178125053` passed, and production release `34178604512` deployed app revision `lyrashield-app--0000319`, scanner revision `lyrashield-scanner--0000292`, and egress-proxy revision `lyrashield-egress-proxy--0000160` at 100% traffic. A fresh post-deployment Codex Desktop task discovered 14 tools: all five mutating schemas expose optional `approvalId`, and none of the nine read-only schemas exposes it. Workspace, target, and Gate v2 reads passed. One unapproved report request failed closed for missing write scope, and no report was created.

Exact-input approved writes passed on all six selected surfaces. Refresh, revocation, permission loss, and upgrade remain incomplete across the full matrix, as recorded in the lifecycle ledger below. None of these partial lifecycle results is promoted to complete desktop acceptance.

The coordinated public artifacts are CLI `0.2.4`, MCP `0.2.5`, Agent Plugin `0.1.24`, and marketplace release `v0.1.24` at marketplace commit `454c60e44304ded2a8f91587fe6ad4f78e0cfe70`.

## Hosted desktop follow-up — 2026-09-08

The founder selected Claude Chat on the existing Free account instead of Claude Code for the remaining Claude acceptance. This changes the tested surface; it does not establish Claude Code acceptance. Claude's official [connector documentation](https://support.claude.com/en/articles/11176164-use-connectors-to-extend-claude-s-capabilities) allows one custom remote connector on Free.

Reversible, user-local configuration changes moved OpenCode, Hermes, Antigravity, and Devin Local to hosted OAuth. Original configurations were backed up; unrelated server entries were retained. No billing environment or billable scan was created. The existing “Staging Site” target is an assessment fixture, not billing infrastructure.

| Desktop surface            | Fresh evidence                                                                                                                                                                                                        | Remaining limitation                                                                                                                                                                                                                         |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OpenCode 1.18.29           | Hosted OAuth login, workspace discovery, browser-approved report `cmtsil8vt000101id23ls8y29`; old approval `cmtsihzbb000001idjbe1g1sk` subsequently denied with “Approval has expired. Request a new approval.”       | Exact-release replay, natural refresh, refresh-token revocation, native reauthentication state, browser reauthorization, and restored connection status passed on app revision `0000321`. Permission loss remains pending.                   |
| Hermes 0.21.1              | Hosted OAuth login, full desktop restart, browser-approved report `cmtsiwbrz000301idkyg0ohm1` using approval `cmtsirhdh000201idg271kk54`; subsequent expired-approval denial and natural access-token refresh passed. | The supported updater advanced Hermes from 0.20.5 (`b96a9b7`) to 0.21.1 (`ee84ccd`). Refresh-token revocation and post-reauthorization read passed through the pinned bridge; permission loss remains pending.                               |
| Antigravity 2.12.2         | Hosted OAuth authentication, workspace discovery, browser-approved report `cmtsjcdob000501idgqaqkofv` using approval `cmtsj3css000401id4y0oy2b3`; subsequent expired-approval denial.                                 | Refresh-token revocation, native denial, automatic reauthorization, and restored workspace read passed through the pinned bridge. Permission loss remains pending. Native update check reported the current version.                         |
| Devin Local 3.8.20         | Native desktop reads and browser-approved report `cmtsjzeeo000101ffc25cnapc` using approval `cmtsjvjp7000001ff0rsjeji7` passed through `mcp-remote@0.8.4`; post-restart workspace read passed.                        | Direct native HTTP OAuth fails with “Authorization response missing required issuer.” Bridge refresh and refresh-token revocation passed; clean restart silently reauthorized and restored the native read. Permission loss remains pending. |
| Codex Desktop 26.901.51231 | After reauthorization and restart, native reads, browser-approved report `cmtskdapl000501ffjehcxfio` using approval `cmtskcjxq000401ffbk2lcm6t`, and changed-input denial passed.                                     | Exact-release replay passed on app revision `0000321`; remaining lifecycle cases are pending.                                                                                                                                                |
| Claude Chat 1.46388.4      | Custom connector OAuth, three native Chat reads, pending approval `cmtsjzioj000201fff8fmcw81`, and user-approved report `cmtsk0bkt000301ffmcdsib7v` passed.                                                           | Refresh, revocation, and permission loss remain pending; native update check reported the current version. This is Claude Chat, not Claude Code.                                                                                             |

OpenCode's automatic registration omitted native-client metadata and was rejected for its HTTP loopback callback. Explicit native-client registration with the exact callback and the documented `oauth.clientId` configuration allowed native desktop OAuth to complete. Hermes declares native-client metadata itself. Devin Local's [official desktop documentation](https://docs.devin.ai/desktop/devin-local#mcp-re-authentication) identifies the native Authenticate control used for its attempted reauthorization.

Two live regressions were fixed through separate reviewed diffs and green CI: PR #624 (`a22fcd1c`) repairs approval links to the Review Queue and anchors the matching card; PR #625 (`4b7c07a6`) retains structured MCP output when replaying an executed approval. The replay defect was an MCP output-schema error, not successful replay protection. The latter passed exact-head CI `34215726883` and merged-main CI `34216415313`, including the database RLS check. Deployment and native replay readback remain separate gates.

Devin's native update check returned to idle without offering an update. Its tested fallback uses the existing stdio server mechanism with `mcp-remote@0.8.4`, HTTP-only transport, explicit native-client registration metadata, and an isolated user-local credential directory. Only LyraShield's entry changed. This is an operational compatibility configuration, not a new product dependency. A forced local-cache-expiry probe did not demonstrate token rotation, but a later grant using that bridge client's refresh token returned HTTP 200 before revocation.

Devin also rejected the consumed approval when its title was changed, returning “Submitted input does not match the requested action.” This establishes the live exact-input binding on the bridged desktop path; no second report was created by that negative test.

Codex's restarted desktop task loaded the renewed write scope and `approvalId` schema. The founder approved the exact developer-report title “Codex browser approval acceptance 2026-09-08”; execution returned the generated report above with structured content. Reusing that approval with a changed title returned “Submitted input does not match the requested action.” All six selected desktop surfaces now have authenticated reads and browser-approved report creation, with Claude Chat explicitly substituted for Claude Code. This does not complete the lifecycle matrix.

A restricted revocation probe used only the isolated Devin bridge client's own access token and public client identifier. The production OAuth revocation endpoint returned HTTP 400 `unsupported_token_type`: “JWT access tokens are self-contained and cannot be revoked server-side.” No token was revoked. The installed Better Auth 1.7.1 implementation explicitly implements that response; the resource server currently validates JWTs locally. Immediate access-token revocation is therefore unsupported by the tested contract, not a passed lifecycle test. Refresh-token revocation, permission loss, and token refresh remain separate unproven cases. No credential values were retained in this receipt.

Claude Chat was fully quit through its native menu, observed stopped, and relaunched from `/Applications/Claude.app`. The existing conversation then completed a fresh authenticated workspace read and rejected approval `cmtsjzioj000201fff8fmcw81` with “Approval has expired. Request a new approval.” This proves restart persistence and natural approval expiry for the selected Chat surface.

Before the replay fix received production traffic, Codex repeated the exact approved report call and received the same report ID `cmtskdapl000501ffjehcxfio`, with no `structuredContent` in the returned result. This establishes idempotent replay on that client, but does not establish acceptance of the structured-output fix.

After Azure promoted `lyrashield-app--0000321` to 100% traffic, the exact Codex replay returned both text and `structuredContent`, containing the same report ID `cmtskdapl000501ffjehcxfio`. The app image was product `4b7c07a6a833a1da40d2da11f6d8f981a41a8f95`, digest `sha256:f81d192dc6265dc4c289b577cb7c4b7bde7063160bc132c7edb9f7fedc10544f`. This establishes the deployed legacy-result reconstruction path in Codex.

OpenCode 1.18.29 repeated the original strict-output-schema failure case after the founder approved request `cmtskqfck000001l9sc4k2jp1`. Both the initial execution and identical replay returned `report_created`, status `generated`, and report ID `cmtsksxzl000101l9kftgk396` for “OpenCode structured replay acceptance 2026-09-08.” The native desktop conversation showed both tool calls and no MCP schema error. This closes the live OpenCode replay regression on app revision `0000321`, product `4b7c07a6`; no additional approval, scan, payment, or file operation was performed.

OpenCode's original OAuth session was more than one 3,600-second access-token lifetime old when that native replay passed. Its credential store had atomically advanced to a new issuer-bound token issued at `2026-09-08T16:43:15+05:30`, expiring at `17:43:15+05:30`, with a refresh token still present. Only claim names, timestamps, issuer, audience, and refresh-token presence were inspected; no credential value was printed or retained. This establishes natural refresh in OpenCode rather than the earlier artificial cache-expiry probe.

Hermes likewise completed a native workspace read after its original 3,600-second access-token lifetime. Its isolated LyraShield token file advanced to an issuer-bound token issued at `2026-09-08T16:49:37+05:30`, expiring at `17:49:37+05:30`, with the refresh token retained. No credential value was printed or retained. This establishes natural refresh in Hermes.

Antigravity's documented direct `serverUrl` OAuth connection stopped exposing LyraShield after the access token expired; the client cleared its token cache instead of refreshing it. Only the LyraShield entry was moved to the existing, version-pinned `mcp-remote@0.8.4` fallback already used for Devin. A forced-expiry probe then completed the provider refresh grant and a native workspace read. The refresh grant may reuse the still-valid access token, so this receipt establishes accepted refresh, not token rotation.

The isolated Antigravity bridge refresh token was then revoked through the production OAuth revocation endpoint. A repeated refresh grant returned `invalid_grant`. With the cached access token made invalid and expired, the restarted desktop no longer exposed the LyraShield server. Clearing only the revoked LyraShield cache triggered OAuth reauthorization through the existing signed-in browser session; the new token had a fresh one-hour lifetime and retained refresh capability, and a subsequent native workspace read passed. This proves refresh-token revocation, desktop denial when no usable access token remains, reauthorization, and recovery. It also confirms the product's current boundary: revoking a refresh token does not invalidate a previously issued self-contained JWT before that access token expires.

OpenCode's isolated refresh token was revoked after a successful control refresh. The next refresh returned HTTP 400 `invalid_grant`; after its cached access token was invalidated and the desktop restarted, the native status changed to “Click to authenticate.” Clearing only the revoked LyraShield entry, completing browser consent, and restarting restored the native connected state. The installed Free model could not send another model-mediated read, so the recovery receipt ends at the native connected status rather than claiming a second tool response.

Hermes' isolated refresh token was revoked after a successful control refresh, and the next refresh returned HTTP 400 `invalid_grant`. The restarted Hermes 0.21.1 desktop required reauthentication. Its native callback IPC dropped the authorization server's RFC 9207 `iss` parameter, so the native OAuth completion failed even though the browser callback contained it. The supported, pinned `mcp-remote@0.8.4` bridge completed reauthorization and a post-restart workspace read. This is a Hermes desktop callback defect, not a LyraShield OAuth failure.

Devin's bridge refresh grant returned HTTP 200 before revocation. Revoking the isolated refresh token returned HTTP 200 and a repeated grant returned HTTP 400 `invalid_grant`. After stopping the full desktop host and its bridge processes, invalidating only the LyraShield cache, and relaunching, Devin silently obtained a fresh session and completed one native workspace read. Because the host did not leave a durable unauthenticated state visible, this proves provider revocation plus clean-host recovery but not a native denial screen.

`mcp-remote@0.8.4` defaults every server identity to loopback port 3846. Running several desktop hosts at once caused one host to own that port and prevented another host's OAuth callback. The local configurations now assign stable, distinct ports: Devin 3847, Antigravity 3848, and Hermes 3849. Each file remains mode 0600 and has a dated backup. This removes the cross-client callback collision without changing product code or package versions.

Native update checks reported no available update for Claude Chat 1.46388.4, Devin Local 3.8.20, Antigravity 2.12.2, and OpenCode 1.18.29. Hermes' supported updater advanced the clean checkout and desktop app from 0.20.5 (`b96a9b7`) to 0.21.1 (`ee84ccd`), relaunched the app, and completed a fresh LyraShield workspace read. Codex's bundled CLI 0.153.4 matched the current published CLI package and automatic desktop updates are enabled, but Codex Desktop cannot be driven through the available native automation surface, so a native “check for updates” receipt is unavailable.

## Lifecycle acceptance ledger

| Desktop surface            | Refresh                                                                  | Revocation                                                                                     | Permission loss                                                                       | Upgrade                                                                 |
| -------------------------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| OpenCode 1.18.29           | Passed: natural token renewal and post-renewal native read               | Passed: refresh-token revoke, `invalid_grant`, native reauthentication state, browser recovery | Blocked on a second controlled workspace member                                       | Passed: native update check reported current                            |
| Hermes 0.21.1              | Passed: natural token renewal and post-renewal native read               | Passed through pinned bridge: revoke, `invalid_grant`, reauthentication, native read           | Blocked on a second controlled workspace member                                       | Passed: 0.20.5 to 0.21.1 and post-upgrade native read                   |
| Antigravity 2.12.2         | Passed through pinned bridge: forced expiry, accepted grant, native read | Passed: refresh-token revoke, `invalid_grant`, native denial, reauthorization, native read     | Blocked on a second controlled workspace member                                       | Passed: native update check reported current                            |
| Devin Local 3.8.20         | Passed through pinned bridge: accepted refresh grant and native read     | Partial: revoke and `invalid_grant` passed; clean restart silently reauthorized and read       | Blocked on a second controlled workspace member                                       | Passed: native update check reported current                            |
| Codex Desktop 26.901.51231 | Pending                                                                  | Pending                                                                                        | Blocked on a second controlled workspace member                                       | Partial: bundled CLI current; native desktop update receipt unavailable |
| Claude Chat 1.46388.4      | Pending                                                                  | Pending                                                                                        | Blocked on a second controlled workspace member; this is Claude Chat, not Claude Code | Passed: native update check reported current                            |

The acceptance workspace currently contains only `ankit@fusionwaveai.com` as `OWNER`. Removing or demoting that sole owner would strand the workspace and would not be a safe permission-loss test. A complete permission-loss receipt requires adding a second controlled account, authenticating that account in each client, then removing or downgrading only that temporary membership and confirming cached and fresh reads and writes fail. No invitation was sent and no existing membership was changed without an identified second account.

Lifecycle acceptance therefore remains incomplete. Upgrade coverage is complete for the controllable hosts except the native Codex update receipt. Refresh is proven for OpenCode, Hermes, Antigravity, and Devin. Revocation is proven for OpenCode, Hermes, and Antigravity; Devin has provider revocation and clean-host recovery without a durable native denial screen. Codex and Claude use host-managed credentials that were not destructively removed from the active sessions. Permission loss remains safely blocked for every host until a second controlled workspace identity exists. These rows are client-specific: one host's lifecycle receipt is not promoted to another host.
