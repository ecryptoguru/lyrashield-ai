# Agent-surface upgrade completion record

Release candidate prepared from `origin/main` `0f87d24b` on branch `codex/agent-surfaces-six`. This record separates source implementation, local evidence, publication, deployment, and live-client proof. The branch and its draft PR are the source review artifact; no production scan, billing event, package publication, or deployment was used as a test.

| Workstream           | Source state                                                                                                                                            | Local evidence                                                                                                                                                                                                                                                         | CI / publication / deployment / live proof                                                                                                                                                          |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A. Distribution      | Candidate versions CLI `0.2.13`, MCP `0.2.10`, Agent Plugin `0.1.30`; registry, installer and marketplace pins aligned.                                 | Isolated Node 24.17.0 tarball install passed CLI help, dry-run, JSON, quality and staged diff; packed MCP stdio initialized with 21 tools and packed HTTP initialization returned 200. Plugin 31 tests; registry 171 tests; marketplace export validated 31 artifacts. | Fresh CI: PENDING. npm and marketplace publication: NOT RUN. Installed-client proof: NOT RUN.                                                                                                       |
| B. Scan lifecycle    | SDK abortable wait; CLI wait/watch/status/resume and explicit cancel; server/MCP cancellation uses separate grant and recorded operation.               | Focused wait, replay, cancellation and worker child-process tests passed; full repository and selected browser gates recorded below.                                                                                                                                   | Fresh CI: PENDING. Deployment and live scan proof: NOT RUN. No paid scan performed.                                                                                                                 |
| C. Preflight         | Typed SDK, CLI, MCP and WebMCP advisory eligibility; POST remains the authoritative admission check.                                                    | Focused eligibility and API parity tests passed; no-mutation and denial cases covered.                                                                                                                                                                                 | Fresh CI: PENDING. Deployment and live-client proof: NOT RUN.                                                                                                                                       |
| D. Workflow parity   | Shared bounded diff analyzer; SDK/CLI/MCP attachment access, approval-bound fix-PR request and recorded-operation recovery.                             | Focused diff, attachment, approval and replay tests passed; security recheck found no remaining actionable defect.                                                                                                                                                     | Fresh CI: PENDING. Deployment, approved production fix PR and live retest: NOT RUN.                                                                                                                 |
| E. MCP compatibility | Official SDK v2 server supports modern `2026-07-28` and legacy clients over stdio/HTTP. Existing scan IDs and AgentOperation recovery remain available. | Protocol compatibility tests and packed exchanges passed.                                                                                                                                                                                                              | **MCP Tasks BLOCKED**: SDK v2.1.0 removed its experimental task receiver, and the modern codec rejects `tasks/*`. Tasks are not advertised. Fresh CI, deployment and live clients: PENDING/NOT RUN. |
| F. WebMCP            | Dashboard preflight/progress/report tools and activity receipts; opt-in bounded runtime fixture and export.                                             | Browser harness 69 tests passed. Chrome 152 native fixture: 7 PASS, 1 INCONCLUSIVE because the browser did not deliver an AbortSignal to the handler; cancellation cannot be claimed.                                                                                  | Owned HTTPS staging and live dashboard/client receipt: NOT RUN. Native cancellation assurance: INCONCLUSIVE.                                                                                        |

## Release candidate artifacts

Unpublished tarballs rebuilt and installed from this candidate are in `/tmp/lyrashield-agent-rc-final`; the exact-source smoke receipt is `receipt.json` there. SHA-256 values are CLI `bb8a83bb1559dd28ba76dd7cc4816e5d6e49eb4cf0888017764b7e74df47dbcd`, MCP `4d7165cf88a517dc0b2732f1e73e1d260df382b41dac5afa221881782dfee85d`, and plugin `82f525d27d1c34fa00ec36f65cdf87f18c36d1babef79da0bc132bb72ae3fec8`. The unpublished marketplace export is `/tmp/lyrashield-marketplace-rc.1gOJeU/export`; a separate marketplace review branch validates all 31 generated artifacts.

The local native-browser fixture receipt is `/tmp/lyrashield-webmcp-fixture-receipt-6.json`, bound to fixture checksum `7855623be3f728d61ddd4c0e6853b050ca679c3c494428e8ff0e815f775f1d47`. It is localhost evidence, not an owned-staging or customer-origin receipt.

## Verification gates

- Full `pnpm test` passed when run alone: 5,223 core tests passed (22 skipped), 291 marketing tests passed, and motion and mocked operations suites passed. An earlier simultaneous lint/test run hit four core and one marketing 5-second timeout; serial core and marketing reruns and the solo full runner passed.
- Repository lint 35/35, typecheck 37/37, formatting, `git diff --check`, and build 11/11 passed with CI-equivalent synthetic build environment. Final browser-harness and E2E checks are recorded separately.
- Disposable PostgreSQL: 103 migrations applied; 47 RLS and 20 focused database tests passed. This is synthetic local evidence, not production billing proof.
- Full Playwright suite: 43/43 passed against disposable PostgreSQL and Redis, including mobile shell, OAuth consent, platform admin, and native WebMCP browser behavior. The first run without isolated Redis hit one admin timeout and Redis `NOAUTH` warnings; the isolated rerun passed.
- Browser harness: 69/69 passed. E2E and browser-harness type checks passed.
- Worker image verifier: BLOCKED by stale local `lyrashieldai-worker:latest`; the exact candidate image has not been built. It cannot count as a pass.
- Markdown lint: advisory failure in unchanged Agent Plugin skill files; not a release gate pass.
- CI, merge, package publication, production deployment, native owned-staging runtime, and live-client/browser proof: NOT RUN.

## Remaining release gates

1. Review and merge only after fresh CI and branch checks are green. Keep E2 Tasks explicitly disabled until the official modern receiver supports the methods and a new implementation passes authorization and recovery tests.
2. Repack from the reviewed commit, record exact hashes, and publish through an authorized release path in dependency order. Verify npm dist-tags and clean installation; do not overwrite published versions.
3. Deploy compatible server/API changes before clients. Record exact SHA, app/worker revision, nonbillable readiness and OAuth checks, then acquire real supported-client receipts.
4. Run the native fixture on an owned HTTPS staging origin. Record cancellation as unsupported or inconclusive until a browser supplies a real handler signal. Do not claim all six complete while this and MCP Tasks remain open.
