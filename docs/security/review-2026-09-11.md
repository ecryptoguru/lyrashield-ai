# Security review — 2026-09-11

**Baseline:** `main` @ `3b289a43` (`3b289a43c601aca7b45b92d06cdc0af386fc958d`).
Reviewed tree: working tree at `d6fc3384` (4 post-baseline commits: 2 docs, 2 small
billing/web fixes — none touch reported locations).
**Scope:** lyrashield-ai monorepo only (`apps/*`, `packages/*`, CI/CD, ops scripts,
docker). Sibling repos (`lyrashield-engine`, `lyrashield-marketplace`) are out of
scope; the engine was reviewed at its TypeScript boundary contract.
**Deliverable:** findings report only — no application code was modified.
**Remediation:** merged in PR #660 as `ddc42df4`; production rollout and live
readback are recorded under [Merge, deployment, and live evidence](#merge-deployment-and-live-evidence).
All 15 confirmed findings and
11 promoted needs-verification items carry fixes with regression coverage;
every needs-verification item has a recorded disposition: 30 fixed or
evidence-closed and 4 accepted with an owner and review date.

**Companion files:**

- `review-2026-09-11.findings.json` — machine-readable ledger (15 confirmed
  findings + 11 promoted needs-verification items with remediation status,
  34 needs-verification dispositions, 3 hardening notes)
- `review-2026-09-11.v16-dedupe.md` — deduplication manifest vs the in-flight
  `fix/deep-review-v16` branch (head `3fc40597`, 224 files)
- `review-2026-09-11/wave-{A..I}.md` — per-wave detail, verified-safe lists, and
  per-item evidence

---

## Executive summary

**15 confirmed findings: 0 Critical, 3 High, 12 Medium.** All 34
needs-verification items now have explicit dispositions: 30 were fixed or closed
with evidence and 4 were accepted with owners and review dates. No external
evidence gates remain. There are also 3 hardening notes.

The three High findings:

| ID             | Domain        | Summary                                                                                                                                                                                                    |
| -------------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **VULN-A-001** | Authorization | Workspace-bound API keys and OAuth tokens read _other_ workspaces on routes/dashboard pages that check membership only — cross-tenant read of targets, projects, member PII                                |
| **VULN-A-002** | Authorization | OAuth bearer sessions bypass credential-type rejection — a read-scoped or delegated token can mint a durable `lsk_` write key, publish public scorecards, and act on the creator's affiliate payout method |
| **VULN-C-001** | Money         | Affiliate 12-month commission cap is unreachable dead code — every renewal mints a full commission for the life of the subscription                                                                        |

A-001 and A-002 share one root cause: `requireWorkspaceAccess` enforces credential
workspace-binding but not the OAuth scope split, and several routes never call it.
Both were re-verified in source during consolidation. C-001 is a confirmed
dead-code defect (`isFirstPayment` has no production writer) with direct financial
impact; cash realization is currently gated by the admin-disabled payout path, but
the liability ledger accrues unconditionally.

The 12 Medium findings cluster into: supply-chain integrity gaps in CI/CD and the
distributed scan workflow (H-001..H-004, E-001, E-002), desktop/Tauri resilience
and license-enforcement issues (F-001..F-004), a fail-open secure-cookie
derivation (I-001), and a live-probe finding that marketing content is served
over plaintext HTTP without redirect (P-001).

No critical or high-confidence injection, RCE, SSRF, auth-bypass, or RLS-failure
vulnerability was identified. The architecture's central invariants — RLS scoping,
webhook signature verification, idempotent money paths, evidence integrity, fix-PR
approval binding, and platform-admin elevation — were tested against their
implementations and held.

---

## Methodology

Hybrid waves per the approved plan:

1. **Phase 0 — Baseline/dedupe.** Pinned `main@3b289a43`; reviewed the full v16
   diff (224 files) and built a deduplication manifest so v16-covered defects are
   not re-reported.
2. **Phase 1 — Automated sweep.** `pnpm audit --prod` (clean), `cargo audit` (no
   known vulns; 8 allowed warnings incl. unmaintained `proc-macro-error`/`unic-*`,
   `glib` unsoundness, yanked `chacha20`), ESLint with security rules across
   workspaces (`--max-warnings 0`, clean), `security_scan.py` (39 hits — all
   false positives after triage: regex `.exec()` misread as process exec,
   security-pattern source text, generated Prisma artifacts, test fixtures,
   dev-only docker credentials, and a bogus missing-lockfile check despite
   `pnpm-lock.yaml`), plus targeted greps (`dangerouslySetInnerHTML`, `eval`,
   `new Function`, raw SQL, `Math.random`, redirects, `child_process`, cookie
   flags, TLS verification, env vars, CORS). Gitleaks/semgrep unavailable
   locally; CI gitleaks config reviewed instead.
3. **Phase 2 — Nine domain waves** (parallel review agents, each tracing
   attacker-controlled input end to end): A HTTP/authn/authz (~180 files,
   all 126 route handlers + `/api/v1` shims), B tenancy/RLS, C money/business
   logic (~95 files), D worker/engine/sandbox, E agent surfaces (~135 files:
   MCP/CLI/SDK/plugin/installers + the server routes they exercise), F
   desktop/Tauri, G marketing, H CI/CD/supply chain, I cross-cutting
   crypto/config/logging.
4. **Phase 3 — Bounded production probes** (~30 requests, read-only): security
   headers on `app.` and apex, unauthenticated protected-route access,
   bearer-only/workspace-header-only/junk-auth against platform-admin endpoints,
   validation-vs-auth ordering, HTTP→HTTPS behavior.
5. **Phase 4 — Consolidation.** All findings re-checked against the v16 manifest;
   the two replacement-wave results were adjudicated where they contradicted the
   original embedded reports (one original "verified safe" claim — CLI diff
   option injection — was overturned and is reported as VULN-E-001 after direct
   source verification).

**Reporting standard:** only high-confidence findings with confirmed
attacker-reachable paths are reported. Test files, dead code, server-controlled
constants, framework-mitigated patterns, and auth-required-merely-because-auth
paths were excluded. Threat actors: T1 external unauthenticated, T2
authenticated/tenant (incl. workspace-credential holders), T3 malicious scan
target, T4 supply chain.

---

## Findings

### VULN-A-001 — High — Workspace-bound credentials read other workspaces on membership-only routes and dashboard pages

- **Location:** `apps/web/src/app/api/targets/route.ts:273-344`,
  `apps/web/src/app/api/projects/route.ts:81-136`,
  `apps/web/src/app/api/team/route.ts:328-397`,
  `apps/web/src/lib/cache.ts:15-41` + `workspace-selection.ts:3-13` feeding every
  `app/(dashboard)/**` page; same defect via `GET /api/v1/targets` and
  `GET /api/v1/projects` re-export shims.
- **Confidence:** High. **Threat actor:** T2.
- **Issue:** `getSession()` falls back to `getBearerSession()`, which populates
  `session.apiKey.workspaceId` / `session.oauth.workspaceId` while `session.userId`
  is the credential's backing user. These routes accept an attacker-controlled
  `workspaceId` and check only `workspaceMember` for `session.userId` — the
  documented binding invariant ("a key minted for workspace A must never
  authorize workspace B", `session.ts:314-316`, enforced at `:317-322`) is never
  invoked. The dashboard variant resolves the active workspace from the unsigned,
  client-settable `activeWorkspaceId` cookie filtered by user memberships;
  `session.apiKey`/`session.oauth` are never consulted.
- **Impact:** A credential bound to workspace A whose backing user is also an
  active member of B reads B's target inventory (URLs, `apiSpecUrl`,
  `repoFullName`, environment), project metadata + `riskScore`, full member
  roster **with emails and pending-invitation emails** (PII), and all rendered
  dashboard pages (findings, scans, reports, billing, settings). PostgreSQL RLS
  does **not** mitigate: the Prisma extension binds `app.current_workspace_id`
  from the query's own attacker-chosen `workspaceId`. Stale-key amplification: a
  key keeps working after its creator leaves A and silently reads the creator's
  remaining workspaces.
- **Evidence:** membership probe then `findMany({workspaceId})` at
  targets:286-311 / projects:93-113 / team:340-392; `cache.ts:28-31`
  `selectActiveWorkspaceId(memberships, cookieStore.get("activeWorkspaceId"))`;
  `workspace-selection.ts:5-9` returns `requestedWorkspaceId` on membership match;
  correct pattern for contrast at `workspaces/route.ts:28-39,129-131`.
  Directly re-verified in source during consolidation.
- **Fix:** Route every workspace-scoped read through
  `requireWorkspaceAccess(workspaceId)`; where `getSession()` is retained, add
  explicit `session.apiKey.workspaceId !== workspaceId → 403` /
  `session.oauth.workspaceId !== workspaceId → 403`; clamp
  `getCachedWorkspaceContext` to the bound workspace when a credential session is
  present.
- **v16 overlap:** none.

### VULN-A-002 — High — OAuth bearer sessions bypass credential-type rejection: read/delegated tokens mint durable `lsk_` write keys and publish public scorecards

- **Location:** `apps/web/src/app/api/api-keys/route.ts:25-30,56-67` (GET/POST),
  `api-keys/[id]/route.ts:18-23` (DELETE), `targets/[id]/scorecard/route.ts:16-20`,
  `scorecards/[id]/route.ts:16-19`, `workspaces/active/route.ts:25-29`,
  `affiliates/api/payouts/method/route.ts:34-73`,
  `affiliates/api/payouts/request/route.ts:13-44`.
- **Confidence:** High. **Threat actor:** T2.
- **Issue:** `requireWorkspaceAccess` enforces workspace binding + membership/role
  only; the read/write scope split and delegated-operation grants live
  exclusively in `requirePermission` (`session.ts:356-380`). Every caller that
  hand-rolls the remaining check tests only `session.apiKey` and forgets
  `session.oauth`. `withCookieMutation` skips the same-origin gate for Bearer
  requests by design, so an OAuth token flows straight through — even on the
  api-keys routes whose docstring promises browser-only management.
- **Impact:** An OAuth token with only `lyrashield.read` — or a delegated
  `agentConnection` constrained by `allowedOperations`/`allowedTargetIds` —
  whose subject is an ADMIN member mints a durable `lsk_` key with
  `scopes: ["read","write"]` (`createApiKey` at `:61-67`, `rawKey` returned once).
  That key satisfies `requirePermission`'s write check across the whole workspace
  API with no connection-level grant limits — self-serve privilege escalation
  plus a persistent credential that survives connection revocation. A
  read-scoped token held by a PUBLISHER can also create **public unauthenticated
  `/score/{slug}` scorecard shares** (externally visible disclosure from a read
  grant), and act on the creator's affiliate payout method / payout requests —
  money-adjacent actions outside the credential's contract.
- **Evidence:** `api-keys/route.ts:57-59` — `if (session.apiKey) → 403`, no
  `session.oauth` branch (re-verified in source); `session.oauth.workspaceId`
  binding at `session.ts:87-117,320-322`; correct both-types rejection for
  contrast at `connection-auth.ts:7`.
- **Fix:** Reject `session.oauth` alongside `session.apiKey` on api-keys and
  affiliate mutation routes; on scorecard publish/revoke and
  `workspaces/active`, call `requirePermission` with a mutating permission or
  extend the check to `session.oauth?.scopes.includes("lyrashield.write")` plus
  delegated-operation gating.
- **v16 overlap:** none (`e40a945b` hardens adjacent consent code, not these
  checks).

### VULN-C-001 — High — Affiliate 12-month commission cap is unreachable dead code; every renewal mints a full commission for the life of the subscription

- **Location:** `packages/affiliate/src/commission/engine.ts:275-313`; producers
  `packages/affiliate/src/webhook-dispatch.ts:185-187`,
  `packages/billing/src/domain-events.ts:408-411`; metadata stamped at
  `apps/web/src/app/billing/checkout/route.ts:129-136`.
- **Confidence:** High (verified end-to-end; consolidation grep confirms
  `isFirstPayment` writers exist only in `*.test.ts`/e2e specs). **Threat
  actor:** T2 (APPROVED affiliate; self-referral check is email-equality only).
- **Issue:** `onOrderPaid` creates the `AffiliateSubscription` row — which
  carries `firstPaidAt`/`capEndsAt` — only inside `else if (isFirstPayment)`
  (engine.ts:289). The flag is read from event metadata but production checkout
  metadata never stamps it. `providerSubscriptionId` is populated, so the
  `findUnique` runs on every event and always misses → `capEndsAt` stays null →
  the `capMonths`/12-month expiry branch (engine.ts:316-364) is unreachable.
- **Impact:** The contractual cap (`capMonths` default 12; public affiliate copy)
  is unenforced — a referred LAUNCH_ASSURANCE annual subscription ($4,188) mints
  ~$1,047 commission **every year, forever**; monthly plans mint on every
  `subscription.charged` up to the 100-year `total_count`. Commissions flow
  `PENDING → AVAILABLE` hourly and accrue as payable liability; cash realization
  is currently gated by the admin-disabled payout path but pays out once that
  gate opens. Side effects: `activeReferrals` never increments (30% tier
  unreachable — affiliates _under_-paid at scale), `Conversion.subscriptionId`
  always null, clawback decrement unreachable.
- **Evidence:** engine.ts:289 (`else if (isFirstPayment)`);
  domain-events.ts:408; checkout metadata shape has no `isFirstPayment`; repo
  grep confirms no production writer.
- **Fix:** `upsert` `AffiliateSubscription` on `providerSubscriptionId` with
  `capEndsAt = firstSeen + capMonths` whenever a paid event arrives (the unique
  key makes it race-safe), then apply the expiry branch on subsequent events;
  backfill from earliest `Conversion.occurredAt`; optionally stamp
  `isFirstPayment` in checkout metadata for determinism.
- **v16 overlap:** none.

### Medium findings (condensed — full detail in wave files and findings.json)

| ID                               | Severity | Location                                                          | Issue                                                                                                                                                                                                                                                                             | Fix                                                                                                         |
| -------------------------------- | -------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| **VULN-E-001**                   | Medium   | `packages/cli/src/diff-core.ts:89,109,141,175`                    | Git option injection: `--base`/`--head` enter `git diff`/`git show` argv with no `--end-of-options` or ref validation → `--base=--output=<path>` writes diff output to arbitrary files (credential store, rc files; content partially attacker-controlled via authored filenames) | `git rev-parse --verify --end-of-options "${ref}^{commit}"`, pass only resolved SHAs; or reject leading `-` |
| **VULN-E-002**                   | Medium   | `packages/cli/src/commands/login.ts:53-57,62`                     | `login --key` embeds the `lsk_` key in argv + shell history (world-readable on multi-user hosts, logged in CI)                                                                                                                                                                    | Remove/deprecate the flag toward stdin/hidden prompt; never use in CI                                       |
| **VULN-H-001**                   | Medium   | `deploy-azure.yml:547-556`, `production-backup.yml:23-35,189-190` | Production secrets in env while `pnpm install` executes dependency build scripts                                                                                                                                                                                                  | Secret-free install step; step-scoped secrets only                                                          |
| **VULN-H-002**                   | Medium   | `ci.yml:620-629`, `production-backup.yml:78-82`                   | `wranglerVersion: "4"` and mutable `postgres:17-alpine` run with prod credentials                                                                                                                                                                                                 | Exact pins / sha256 digests                                                                                 |
| **VULN-H-003**                   | Medium   | `pnpm-workspace.yaml:76`                                          | `minimumReleaseAge: 7` means 7 **minutes**, not days — publish-age gate effectively absent                                                                                                                                                                                        | `minimumReleaseAge: 10080`                                                                                  |
| **VULN-H-004** (dup. VULN-E-003) | Medium   | `lyrashield-scan.yml:156,172-178`                                 | Distributed `workflow_call` workflow runs unpinned `pip install safety` / `npx pnpm` inside consumers' CI (job token has `security-events: write`)                                                                                                                                | Pin by version + hash / vendored pinned image                                                               |
| **VULN-I-001**                   | Medium   | `packages/auth/src/auth.ts:27`, `env.ts:26`                       | `secureCookies` derives from `BETTER_AUTH_URL` scheme; schema accepts any URL → prod typo ships session cookies without `Secure`                                                                                                                                                  | Require `https:` for `BETTER_AUTH_URL` in production                                                        |
| **VULN-F-001**                   | Medium   | `desktop/…/license/mod.rs:405-426`                                | Unsigned `last_server_verified_at` in user-writable `license.json` → revoked license + blocked API + rewritten timestamp = perpetual offline grace                                                                                                                                | Server-signed revalidation receipt / keychain-held HMAC / cap accumulated offline time                      |
| **VULN-F-002**                   | Medium   | `desktop/…/scan/runner.rs:437-504`                                | Engine stdout/stderr ingested with no line/rate/total cap → hostile scan output causes OOM/disk fill/IPC flood                                                                                                                                                                    | Line truncation, byte budgets, event caps, bounded total stream                                             |
| **VULN-F-003**                   | Medium   | `desktop/src-tauri/src/api.rs`                                    | Unbounded `resp.text()`; non-2xx bodies echoed into error strings shown in UI                                                                                                                                                                                                     | Hard byte cap; never echo raw bodies                                                                        |
| **VULN-F-004**                   | Medium   | `desktop/…/byok/mod.rs:75-82`                                     | `mask_key` byte-slices UTF-8 → panic on multibyte BYOK keys → persistent IPC failure loop                                                                                                                                                                                         | Require `is_ascii()`; `chars().take(4)` masking                                                             |
| **VULN-P-001**                   | Medium   | `apps/marketing/src/middleware.ts:52-55`                          | Live probe: `http://lyrashieldai.com/` and `/pricing` return 200 with content (no redirect); middleware anticipates HTTP rather than redirecting; zone "Always Use HTTPS" not in effect                                                                                           | Cloudflare Always-Use-HTTPS or `301` on `http:`                                                             |

---

## Needs verification (34 items — not reported as vulnerabilities)

These are medium-confidence, deployment-dependent, or sibling-repo-bound items.
Full context in the wave files.

- **Authorization:** VERIFY-A-001 `getClientIP` trusted-header spoofability
  (**closed by production ingress and Azure rightmost-hop evidence**);
  VERIFY-A-002 `input:true` session fields client-writability through Better
  Auth; VERIFY-A-004 user-scope mutations via workspace-bound credentials
  (design question); VERIFY-A-006 API-key validity after creator membership
  removal.
- **Rate limiting:** VERIFY-A-005 in-memory fallback is per-instance under
  Upstash outage (also flagged by Wave E).
- **Tenancy:** VERIFY-B-001 account-deletion actor-attribution query runs
  without workspace context (returns `[]` under strict RLS — orphan-actor
  workspaces skipped); VERIFY-B-002 license-family `WITH CHECK` permits
  `workspaceId IS NULL` writes (latent — no plain-client write path exists).
- **Money:** VERIFY-A-003 whether operator approval gates RazorpayX
  disbursement (**closed: production admission and credentials are absent, so
  the provider path fails closed**); VERIFY-C-001 stale signed-event regression of subscription
  state; VERIFY-C-002 mid-cycle plan change grants a second full pool;
  VERIFY-C-003 `MinutePack.workspaceId` `SetNull` vs workspace-keyed
  credit/refund lookups (latent — no user-reachable hard-delete).
- **Worker boundary:** VERIFY-D-002 `isValidGitRef` accepts leading `-`;
  VERIFY-D-003 engine argv lacks `--` separator; VERIFY-D-004 sandbox docker
  flags are engine-internal (sibling repo); VERIFY-D-005 engine-reported usage
  is the sole billing meter.
- **Agent surfaces:** VERIFY-E-001 legacy credentials without `apiUrl`;
  VERIFY-E-002 unbounded `expiresAt` on agent-approvals; VERIFY-E-003
  client-chosen `expiresAt` on connections; VERIFY-E-004 stale READY verdicts
  still exit 0 in `gate --verdict`; VERIFY-E-005 `safety check` `-r`/index
  directive following; VERIFY-E-006 latent registry containment assumptions;
  VERIFY-E-007 `vendorCli.args` trusted verbatim from the registry package;
  VERIFY-E-008 `@lyrashield/mcp@0.2.8` tag→integrity trust.
- **Marketing:** VERIFY-G-001 `set:html={disclaimer}` on author-controlled
  frontmatter (stored XSS only if authored outside reviewed commits);
  VERIFY-G-002 www→apex redirect — **resolved** by live probe (301 confirmed).
- **CI/CD:** VERIFY-H-001 environment reviewers (**closed by GitHub API
  readback/configuration**); VERIFY-H-002 scanner Container App secret footprint
  (**closed by dependency tracing and GitHub-secret removal**); VERIFY-H-003
  engine-repo visibility (**closed by public repository and distribution-contract
  evidence**); VERIFY-H-004 `ADMISSION_CONFIG_TOKEN` (**closed by replacement
  with a run-scoped `github.token`**).
- **Cross-cutting:** VERIFY-I-001 Better Auth TOTP at-rest encryption;
  VERIFY-I-002 logger marker gaps (`api_key`, `jwt`, `bearer`, `passphrase`,
  `sessionid`…); VERIFY-I-003 `Error.message` not secret-scanned;
  VERIFY-I-004 `NODE_ENV` default in non-Next runtimes; VERIFY-I-005 dead
  `LYRASHIELD_MCP_ALLOW_*` env vars.

## Hardening notes

- **HARDEN-C-001:** derive `WebhookEvent.externalId` from the signed body, not
  the unsigned `X-Razorpay-Event-ID` header (verified non-exploitable for money;
  residual is duplicate bookkeeping rows).
- **HARDEN-D-001:** reject `..`/`.`/control chars in fix-diff `normalizePath` and
  `encodeURIComponent` the write path (verified fail-closed today — the encoded
  read precedes every write and GitHub 404s literal `..`; live-tested).
- **HARDEN-E-001:** hash-pin `@lyrashield/mcp` installs / `vendorCli.args` at the
  distribution boundary (standard npm trust model; flagged because T4 is in
  scope).

---

## Production probe results (Phase 3 — bounded, read-only)

- **App origin** (`app.lyrashieldai.com`): `x-frame-options: DENY`,
  `x-content-type-options: nosniff`, HSTS + preload, CSP with nonce +
  `strict-dynamic`, `frame-ancestors 'none'`, `object-src 'none'`,
  `base-uri 'self'`.
- **Marketing origin**: HSTS, CSP, COOP, XFO, nosniff, restrictive
  permissions-policy — but serves full content over plaintext HTTP
  (→ VULN-P-001); `www` → apex 301 confirmed; `http://app.` correctly 301s.
- **Auth boundaries:** unauthenticated `/api/scans`, `/api/findings`,
  `/api/v1/scans`, `/api/api-keys` return `400` on missing required params
  (validation before auth), then `401`/`UNAUTHORIZED` once a `workspaceId` is
  supplied — no oracle beyond generic auth rejection. Platform-admin endpoints
  reject bearer-only, workspace-header-only, and junk credentials with `401`.

## Verified safe (condensed; per-wave detail in wave files)

- **Session/credentials:** `lsk_` SHA-256 hash-only storage, timing-safe compare,
  narrow SECURITY DEFINER lookup, revoked/expired fail closed; OAuth
  issuer/audience-pinned JWT + live DB re-validation (connection status,
  workspace/user/client/scope/expiry/`authorizationVersion`); workspace binding
  enforced wherever `requireWorkspaceAccess`/`requirePermission` is used — the
  gap is the routes that don't call it (A-001/A-002), not the primitives.
- **CSRF/origin:** cookie-first session resolution (forged Bearer can't exempt a
  browser session); `assertSameOriginMutation` fails closed on contradictory
  `sec-fetch-site`/`Origin`.
- **Webhooks:** GitHub `x-hub-signature-256` fail-closed + delivery-ID dedupe;
  Polar Standard-Webhooks HMAC + 5-min tolerance; Razorpay HMAC over raw body
  with secret rotation + 15-day provider replay bound; unique-constraint
  arbitration + durable per-track processing.
- **Tenancy/RLS:** workspace-scoped model parity, child-table RLS, account-level
  billing isolation, audit-chain locking/hash integrity, system-client call-site
  discipline, no unsafe raw SQL.
- **Money:** `Decimal(19,4)` throughout; idempotency on webhooks/usage/packs/
  refunds/commissions/payouts; advisory locks in fixed order; conditional
  debits; sponsor from persisted state never client input; catalog/quote
  binding; trial/grace caps; clawback integrity; payout CAS + provider
  idempotency; complimentary-grant controls.
- **Worker/sandbox:** BullMQ authority rebinding to persisted scan data; no-shell
  subprocess execution; bounded engine runtime/output; SSRF + egress proxy
  deny-by-default; evidence AES-256-GCM + checksums; retest binds stored
  manifests — engine-only absence is `INCONCLUSIVE`; fix-PR requires human
  approval + checksum + base-commit + per-file fail-closed diff application.
- **Agent surfaces:** credential store `0700`/`0600` + atomic writes + origin
  guard; MCP mutations blocked by default + prompt-injection guard before
  authorization + atomic approval claim; HMAC consent/connection/install state;
  installer allowlists + symlink rejection + atomic merges; SDK `/api/v1`-only +
  idempotent-retry-only.
- **Platform admin:** allowlist + verified `PLATFORM_OPERATOR` + TOTP + recent
  elevation + action nonces; browser-cookie-only; admin console read-only with
  affiliate mutations disabled pending atomic audit.
- **Crypto/config:** Ed25519 license/report signing; CSPRNG tokens; constant-time
  HMAC compares; health/readiness minimal-payload; error-response redaction.

## Limitations and evidence boundaries

- This is a point-in-time code review against `main@3b289a43` (working tree
  `d6fc3384`), not a certification; absence of a finding does not imply
  universal coverage.
- Sibling repos were reviewed only at their boundary in the original pass;
  remediation re-verified `lyrashield-engine` at `4b8db2ee` for
  VERIFY-D-003/004 (see Remediation status). Marketplace artifact generation
  remains boundary-reviewed only.
- Production probes were deliberately bounded (~30 requests, no mutations);
  rate-limit and cache behavior under real load was not exercised.
- Deployment-side configuration (Cloudflare zone settings, GitHub environment
  protection rules, Azure RBAC) is not visible in the repository; related items
  are listed under Needs Verification.
- Three original wave agents lost connection mid-run; their embedded partial
  reports were superseded by replacement agents whose results were re-verified
  during consolidation (one overturned claim → VULN-E-001).
- 4 commits landed on `main` after the baseline was pinned; none touch the
  reported locations (verified via `git diff --stat`).

## v16 deduplication statement

Every finding was checked against `review-2026-09-11.v16-dedupe.md` (224-file
diff, head `3fc40597`). No finding duplicates a v16 fix. v16-touched files that
interact with findings (`session.ts`, `api-auth.ts`, connection code) were
re-read on `main` — the hardened behavior is already present and the defects
reported are in code v16 does not change.

---

## Remediation status

Remediation merged through PR #660 (base `d6fc3384`, head `9eff3b30`, merge
`ddc42df4`). The focused commits include `6963b559` auth/credential boundaries, `8667896b`
billing/affiliate/account ledger, `e46eaef4` CLI, `cb206b6c` CI/cookie/logger,
`9b2c06e3` desktop, `bb1537d0` marketing HTTPS, `621d58a8` needs-verification
closures, `7f3591c4` signed Desktop revalidation receipts, and `421b4d71`
fail-closed Polar event ordering. Every change carries focused regression
coverage; security-sensitive paths fail closed.

### Merge, deployment, and live evidence

- **Merged:** PR #660 head `9eff3b30ebd20a36e6767f3f7eaced92bcefbaef`
  merged to `main` as `ddc42df4dd75076750ff49da08346f9d37369924`.
- **CI and marketing deployment:** main CI `34614103823` passed, including
  migrations/RLS, lint, formatting, typecheck, tests, build, dependency and
  secret scans, Desktop Rust/frontend, marketing browser E2E, app browser E2E,
  Cloudflare deployment, production route smoke, exact live-revision readback,
  and Lighthouse (performance 0.81, accessibility 1.00, SEO 1.00). The live
  marketing revision is `ddc42df4dd75076750ff49da08346f9d37369924`.
- **Azure deployment:** release `34615053797` deployed the same product revision
  with engine pin `b9193d8ed7dd3e73791dcc3ad04b35c0bdc22d58`. App
  `lyrashield-app--0000346` and scanner `lyrashield-scanner--0000319` use web
  digest `sha256:51bdda6681b6e256cd4d4d44bb816dd418a720e8b6e76b1b734df5f94b7acd7e`;
  egress proxy `lyrashield-egress-proxy--0000187` uses
  `sha256:ba88b23be0e6d9a88668d6a10596869fec9715b0b94a3c08f4436ee08ac0ef03`.
  All three are latest-ready and receive 100% traffic. VM worker promotion passed
  for digest `sha256:1a8bfe26acc776257052effb5548007a5839a81ee49e6b94ecb5610cea356f0e`.
- **Live readback:** after enabling Cloudflare zone **Always Use HTTPS**,
  cache-busted probes returned `301` for HTTP apex `/` and `/pricing`, `301` for
  HTTP `www` to HTTPS `www`, and `301` for HTTPS `www` to the apex. HTTPS apex
  returned `200` with the exact marketing revision above. App `/api/health`,
  `/api/ready`, and `/api/ready/scans` returned `200`; readiness reported database,
  Redis, and worker checks true.
- **Post-ledger deployment:** merging the first evidence-ledger update in PR #661
  advanced `main` to `4c2a3151bbacd5c4552579f01ff0d4cbe051fb3a` and intentionally
  produced a fresh, separately verified rollout. Main CI `34619122996` and
  production release `34619783601` passed. Cloudflare serves marketing revision
  `4c2a3151bbacd5c4552579f01ff0d4cbe051fb3a`. Azure app
  `lyrashield-app--0000347` and scanner `lyrashield-scanner--0000320` use web
  digest `sha256:7a3a31a9ccc7a7e59cc91977079bca823dddb51ed59353199d8a86b6a80a6697`;
  egress proxy `lyrashield-egress-proxy--0000188` uses
  `sha256:f8b2a146e7f3793a10a937dd286f26dc7bb494a346475ba52ea719162d3bdceb`.
  All three are latest-ready at 100% traffic. VM worker promotion passed for
  digest `sha256:0e45d8a674243b88d3b2b0d79145884b21af7c7a3dbb6c744e1241978a48042a`
  with engine pin `b9193d8ed7dd3e73791dcc3ad04b35c0bdc22d58`. Fresh cache-busted
  probes reconfirmed HTTP apex `/` and `/pricing` → HTTPS `301`, HTTP `www` →
  HTTPS `www` `301`, HTTPS `www` → apex `301`, HTTPS apex `200` with the exact
  revision, and app health/readiness/scanner-readiness `200` with database,
  Redis, and worker checks true.
- **Platform evidence gates closed:** PR #663 and direct platform readback closed
  VERIFY-A-001, VERIFY-A-003, and VERIFY-H-001..H-004. The app origin requires
  mTLS and rejects direct unauthenticated origin traffic; the public scanner
  consumes Azure Container Apps' documented rightmost `X-Forwarded-For` value.
  Production has no RazorpayX/Payoneer payout admission values or credentials.
  All three GitHub deployment environments now require review, the engine's
  public distribution boundary is explicit, scanner GitHub App secrets are
  removed, and billing admission uses a run-scoped GitHub token. These closures
  do not authorize future payout admission; enabling it requires a new provider
  and operator-control review.
- **Six-gate rollout receipt:** PR #663 merged as
  `32d2ebdf8b3c854a69fad8eece2c7ecdcd76c635`. Main CI `34624618298` passed,
  including migrations, lint, formatting, type checks, affected tests, Azure
  deployment tests, builds, browser E2E, SCA/secret scanning, and the protected
  Cloudflare deployment. Cloudflare version
  `04b429e6-2920-422d-8b1a-cdfe48cbd3b8` serves that exact marketing revision;
  Lighthouse measured performance 0.95, accessibility 1.00, and SEO 1.00.
  Protected release `34625290964` deployed app
  `lyrashield-app--0000348` and scanner `lyrashield-scanner--0000323` on web
  digest `sha256:725986fac8d92c2396f6f2a4263842e6fde2ae42f4cc6e20aa5067aabab850ba`,
  egress proxy `lyrashield-egress-proxy--0000189` on digest
  `sha256:a7d44eb6d4a0ceecb02a410e549be5afdc6e60befb9e88237fdbde9d4e62fabb`,
  and worker digest
  `sha256:1b548791251f5f55fa1d9d92aadc4b9d5c1b8c3a6ad2e17999822dbdcf2484b5`
  with engine pin `b9193d8ed7dd3e73791dcc3ad04b35c0bdc22d58`. All Container Apps are
  latest-ready at 100% traffic with one active rollback revision. Production
  readback found no scanner GitHub App environment references or secret objects,
  no RazorpayX/Payoneer payout configuration on app or scanner, and no
  `ADMISSION_CONFIG_TOKEN` GitHub environment secret. App health, app readiness,
  scan readiness, and scanner readiness returned `200`; the direct app origin
  rejected a client without its certificate. Fresh probes reconfirmed HTTP apex
  → HTTPS apex `301`, HTTPS `www` → HTTPS apex `301`, and the exact live
  marketing revision.

### Confirmed findings — all fixed

| ID                | Commit     | Fix summary                                                                                                                                                              |
| ----------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| VULN-A-001        | `6963b559` | `requireWorkspaceAccess` on targets/projects/team; dashboard workspace resolution re-validates membership; bound credentials denied outside their workspace              |
| VULN-A-002        | `6963b559` | `assertBrowserSession` on browser-only mutations; OAuth scope/delegation enforced; affiliate routes reject workspace-bound credentials                                   |
| VULN-C-001        | `8667896b` | Commission cap enforced at accrual; payout admission remains disabled pending atomic audit                                                                               |
| VULN-E-001        | `e46eaef4` | Ref validation rejects leading-dash/control chars; `rev-parse --verify --end-of-options`                                                                                 |
| VULN-E-002        | `e46eaef4` | `login --key` removed; browser OAuth + keychain only                                                                                                                     |
| VULN-H-001..H-004 | `cb206b6c` | Secret-free `pnpm install`; step-scoped secrets; exact wrangler pin; digest-pinned docker images; minimumReleaseAge corrected                                            |
| VULN-I-001        | `cb206b6c` | Production `BETTER_AUTH_URL` must be `https:` — secure cookies fail closed                                                                                               |
| VULN-F-001        | `7f3591c4` | Server-signed Ed25519 revalidation receipt binds `licenseId`, exact license signature, verification time, and seven-day expiry; Desktop verifies it before offline grace |
| VULN-F-002        | `9b2c06e3` | Bounded engine-output reader — 64 KiB line / 32 MiB stream / 50k events; budget trip kills the engine                                                                    |
| VULN-F-003        | `9b2c06e3` | 4 MiB chunked response cap; untrusted bodies never reach error strings                                                                                                   |
| VULN-F-004        | `9b2c06e3` | `mask_key` is char-boundary safe; BYOK keys must be ASCII                                                                                                                |
| VULN-P-001        | `bb1537d0` | Cloudflare Always Use HTTPS is enabled and live-proven for static and dynamic paths; the worker/middleware guards remain defense in depth                                |

### Promoted needs-verification items — all fixed

| Promoted → Finding        | Commit                 |
| ------------------------- | ---------------------- |
| VERIFY-A-004 → VULN-A-003 | `6963b559`             |
| VERIFY-B-001 → VULN-B-003 | `8667896b`             |
| VERIFY-B-002 → VULN-B-004 | `8667896b`             |
| VERIFY-C-001 → VULN-C-002 | `8667896b`, `421b4d71` |
| VERIFY-C-002 → VULN-C-003 | `8667896b`             |
| VERIFY-C-003 → VULN-C-004 | `8667896b`             |
| VERIFY-D-002 → VULN-D-002 | `e46eaef4`             |
| VERIFY-E-004 → VULN-E-005 | `e46eaef4`             |
| VERIFY-E-006 → VULN-E-006 | `e46eaef4`             |
| VERIFY-I-002 → VULN-I-002 | `cb206b6c`             |
| VERIFY-I-005 → VULN-I-004 | `cb206b6c`             |

### Needs-verification dispositions

- **Closed with code fix (`621d58a8`):** VERIFY-A-002 (update-session
  before-hook validates workspace membership + connection ownership),
  VERIFY-A-006 (verifyApiKey requires the creator's ACTIVE membership),
  VERIFY-E-001 (legacy credentials without `apiUrl` bind to the default
  origin), VERIFY-E-007 (vendor argv exact-match allowlist),
  VERIFY-E-002/E-003 (server-side expiry caps — `6963b559`),
  VERIFY-E-005 (sanitized requirements for `safety check` — `cb206b6c`),
  VERIFY-I-003/I-004 (logger/error scrubbing + NODE_ENV — `cb206b6c`).
- **Closed with evidence:** VERIFY-D-003 and VERIFY-D-004 — sibling
  `lyrashield-engine@4b8db2ee` inspected: argparse rejects leading-dash
  branches and worker argv has no positionals; sandbox containers attach
  only to a `--internal` deny-by-default network (fail-closed admission),
  carry no docker.sock, read-only bind mounts, and mem/cpu/pids/log limits.
  Residual hardening: no `cap_drop ALL` baseline or read-only rootfs.
  VERIFY-I-001 — better-auth 1.7.1 symmetric-encrypts TOTP secrets and
  backup codes at rest. VERIFY-G-002 — live probe confirmed www→apex 301.
- **Accepted with owner + review date 2026-10-11:** VERIFY-A-005
  (per-instance rate-limit fallback under Upstash outage — store now bounded
  at 50k keys + overflow bucket), VERIFY-D-005 (engine is the sole usage
  meter by design; worker-side bounds fail closed), VERIFY-E-008
  (`@lyrashield/mcp@0.2.8` npm version is immutable), VERIFY-G-001
  (disclaimers are reviewed-commit content; sanitize if a non-PR authoring
  path is ever added).
- **Closed platform evidence gates:** VERIFY-A-001 and VERIFY-A-003 are
  evidence-closed. VERIFY-H-001 and VERIFY-H-003 are configuration/evidence-closed;
  VERIFY-H-002 and VERIFY-H-004 are fixed by `e9162054`. Production readbacks and
  the exact dispositions are retained in the machine-readable ledger. No external
  evidence gate remains from this review.
